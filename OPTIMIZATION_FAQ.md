# ❓ FAQ: Оптимизация параметров стратегии

## 1. Сколько сделок показано в результатах?

### Текущий вывод:

В топ-10 результатов показываются только **лучшие по PnL** комбинации параметров.

**Пример:**
```
RSI=17, EMA=10, SL=3%, TP=6% → PnL=6.14 USDT, Trades=1
RSI=17, EMA=10, SL=5%, TP=6% → PnL=6.14 USDT, Trades=1
RSI=17, EMA=10, SL=7%, TP=6% → PnL=6.14 USDT, Trades=1
...
```

**Если у вас 3 сделки:**
- ✅ Это означает, что есть параметры с большим количеством сделок
- ✅ Это лучше для статистической значимости
- ⚠️ Но топ-10 сортируется по PnL, а не по количеству сделок

**Как увидеть все результаты:**

Сейчас в команде показываются только топ-10. Если нужно больше:
- Сортируйте по `--sort-by=trades` для параметров с большим количеством сделок
- Или добавьте вывод всех результатов (требует изменения кода)

---

## 2. Будет ли `strategy:optimize` использоваться на кроне для автоматической оптимизации?

### ❌ НЕТ - текущая концепция:

**`strategy:optimize`** - это **инструмент для ручного использования**, не для автоматической оптимизации на кроне.

**Текущий рабочий процесс:**

```
1. Ручной запуск оптимизации
   ↓
   php artisan strategy:optimize BTCUSDT --timeframe=1h --period=300
   
2. Анализ результатов
   ↓
   Выбор лучших параметров из топ-10
   
3. Ручное применение параметров
   ↓
   - Создание бота с оптимальными параметрами
   - Или обновление существующего бота
   
4. Тестирование на практике
   ↓
   - Активировать в тестовом режиме (dry_run)
   - Мониторить несколько дней/недель
   
5. Реальная торговля
   ↓
   Если результаты хорошие → активировать реальную торговлю
```

---

## 🤔 Почему не автоматически на кроне?

### Проблемы автоматической оптимизации:

1. **Переоптимизация (Overfitting)**
   - Автоматическая смена параметров каждый день → **переоптимизация**
   - Параметры могут работать на исторических данных, но не на будущих

2. **Статистическая значимость**
   - Нужно **достаточно времени** для проверки параметров
   - Менять параметры каждый день → нет времени для валидации

3. **Рыночные условия**
   - Разные параметры работают в разных рыночных условиях
   - Быстрая смена параметров → нет времени адаптироваться

4. **Сложность реализации**
   - Сохранение результатов оптимизации
   - Сравнение с текущими параметрами
   - Автоматическое обновление параметров бота в БД
   - A/B тестирование (одновременный запуск нескольких ботов с разными параметрами)

---

## ✅ Рекомендуемый подход:

### 1. Периодическая ручная оптимизация (раз в месяц/квартал):

```bash
# Раз в месяц запускаете оптимизацию
php artisan strategy:optimize BTCUSDT --timeframe=1h --period=300

# Анализируете результаты
# Применяете лучшие параметры, если они значительно лучше текущих
```

### 2. Мониторинг текущих параметров:

```bash
# Регулярный мониторинг через dashboard/Telegram
php artisan bots:run  # Через cron каждые 5 минут
php artisan telegram:daily-stats  # Ежедневная статистика
```

### 3. Обновление параметров только при необходимости:

- ✅ Если текущие параметры показывают плохие результаты → оптимизация
- ✅ Если рынок изменился (бычий → медвежий) → оптимизация
- ✅ Периодически (раз в месяц/квартал) → оптимизация для проверки

---

## 🚀 Что можно автоматизировать?

### Текущая автоматизация (уже есть):

1. **Запуск ботов:**
   ```cron
   */5 * * * * cd /var/www/bybit && php artisan bots:run
   ```

2. **Синхронизация ордеров:**
   ```cron
   * * * * * cd /var/www/bybit && php artisan orders:sync
   ```

3. **Ежедневная статистика:**
   ```cron
   0 9 * * * cd /var/www/bybit && php artisan telegram:daily-stats
   ```

### Что можно добавить (но с осторожностью):

1. **Автоматическое обновление SL/TP** на основе волатильности:
   ```php
   // Команда для расчета оптимального SL/TP на основе ATR
   php artisan strategy:calculate-sl-tp --bot=24
   ```

2. **Мониторинг производительности и предупреждения:**
   ```php
   // Команда для проверки производительности ботов
   php artisan bots:check-performance
   // Предупреждение, если Win Rate < 40% или PnL < 0
   ```

3. **Автоматическое отключение ботов при плохих результатах:**
   ```php
   // Команда для отключения ботов с плохими результатами
   php artisan bots:disable-poor-performers
   ```

---

## 💡 Рекомендации:

### ✅ Делать:

1. **Периодическая ручная оптимизация** (раз в месяц/квартал)
2. **Мониторинг результатов** через dashboard/Telegram
3. **Обновление параметров** только при необходимости
4. **Тестирование новых параметров** в тестовом режиме перед реальной торговлей

### ❌ НЕ делать:

1. ❌ Автоматическая оптимизация каждый день на кроне
2. ❌ Автоматическая смена параметров без тестирования
3. ❌ Частая смена параметров (меньше чем раз в неделю)

---

## 🎯 Итоговая схема:

```
┌─────────────────────────────────────────────────┐
│ Периодическая оптимизация (раз в месяц)        │
│ php artisan strategy:optimize ...               │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│ Анализ результатов                              │
│ - Выбор лучших параметров                       │
│ - Сравнение с текущими                          │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│ Применение параметров (вручную)                │
│ - Создание/обновление бота                      │
│ - Тестовый режим                                │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│ Мониторинг (автоматически через cron)          │
│ - bots:run (каждые 5 минут)                     │
│ - orders:sync (каждую минуту)                   │
│ - telegram:daily-stats (ежедневно)              │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│ Реальная торговля                               │
│ (если тестовые результаты хорошие)              │
└─────────────────────────────────────────────────┘
```

---

## 10. Бэктест: все результаты отрицательные — что делать?

### Когда видите такое:

- **Spot:** Return от -0.06% до -0.32%, Win Rate &lt; 50%, Total PnL отрицательный.
- **Futures:** 1–2 сделки за 500 свечей, большой минус по PnL.
- **BTC-quote:** Trades: 0, PnL: -3.30 USDT.

### Это нормально?

1. **Спот** — на последних 500 свечах стратегия RSI+EMA часто даёт минус в боковике/тренде. Бэктест не гарантирует прибыль, он показывает, как стратегия вела бы себя на истории.
2. **Фьючерсы** — в бэктесте при закрытии позиции маржа возвращается на баланс; один убыточный вход даёт большой минус к балансу 1000 USDT, поэтому Return % выглядит сильно отрицательным. Уменьшите `position_size_usdt` у фьючерсных ботов, если в реале рискуете меньшей суммой.
3. **BTC-quote, 0 сделок и минус в USDT** — значит за период был только вход (BUY), а сигнала SELL не было; минус — это нереализованный убыток открытой позиции на конец периода (unrealized PnL в USDT).

### Что менять в коде/настройках?

| Действие | Когда |
|----------|--------|
| **Ничего не менять** | Если в реале уже включены консервативные настройки (пауза по дневному убытку, меньший размер позиции, SL/TP) и вы просто смотрите «как бы было» на истории. |
| **Уменьшить размер позиции** | Фьючерсы/btc-quote: в админке снизить `position_size_usdt` / `position_size_btc`, чтобы один убыточный вход не давал −10–15% к депозиту. |
| **Запустить оптимизацию** | Чтобы подобрать RSI/EMA/SL/TP под другой период или таймфрейм: `php artisan strategy:optimize-all` и т.д. |
| **Не полагаться только на бэктест** | Результаты за 500 свечей не предсказывают будущее; используйте бэктест как один из аргументов, а не как единственный критерий включения стратегии. |

Итог: **обязательно что-то менять не нужно**. Если живая торговля идёт с ограничением рисков (малый размер позиции, пауза при убытках), можно оставить как есть и лишь периодически перезапускать бэктесты и оптимизацию для анализа.

---

## ❓ Вопросы?

Если нужна автоматическая оптимизация на кроне - это отдельная задача, которая требует:

1. Хранение истории оптимизаций в БД
2. Сравнение результатов разных периодов
3. Автоматическое обновление параметров ботов
4. A/B тестирование (несколько ботов с разными параметрами)
5. Защиту от переоптимизации

**Это сложная задача, которую лучше делать вручную с периодичностью раз в месяц/квартал.**

---

## 11. Опции strategy:optimize-all (период, таймфрейм, фильтры)

Команда `php artisan strategy:optimize-all` поддерживает:

- **--period=1500** (или 2000) — больше свечей для оценки (по умолчанию 1000).
- **--timeframe=4h** (или 1D) — переопределить таймфрейм для всех ботов при оптимизации.
- **--trend-filter** — включить в бэктесте фильтр тренда (BUY только если цена выше длинной EMA); параметры берутся из config.
- **--volume-filter** — включить фильтр объёма в бэктесте.

Примеры:

```bash
php artisan strategy:optimize-all --period=1500
php artisan strategy:optimize-all --period=1000 --trend-filter --volume-filter
php artisan strategy:optimize-all --period=500 --timeframe=4h
```

Подробнее: `docs/STRATEGY_TUNING.md`, раздел 10.
