# 📁 Расположение логов на сервере

## ⚠️ Боты перестали запускаться — где смотреть

1. **Логи приложения (ошибки и предупреждения):**
   ```bash
   # Последние строки (чаще всего тут видна причина)
   tail -n 200 /var/www/trading-bot/storage/logs/laravel.log

   # Или с датой, если включён daily driver (LOG_CHANNEL=daily):
   tail -n 200 /var/www/trading-bot/storage/logs/laravel-$(date +%Y-%m-%d).log

   # Следить в реальном времени
   tail -f /var/www/trading-bot/storage/logs/laravel.log
   ```

2. **Запуск ботов вручную (сразу увидишь ошибку в консоли):**
   ```bash
   cd /var/www/trading-bot
   php artisan bots:run
   php artisan futures:run
   php artisan btc-quote:run
   ```
   Если команда падает с исключением — текст ошибки будет в выводе и попадёт в `storage/logs/laravel.log`.

3. **Проверить, что cron вызывает планировщик:**
   ```bash
   crontab -l
   ```
   Должна быть строка вида:  
   `* * * * * cd /var/www/trading-bot && php artisan schedule:run >> /dev/null 2>&1`  
   Если её нет — боты по расписанию не запускаются. Добавь через `crontab -e`.

4. **Проверить, что планировщик выполняет команды:**
   ```bash
   cd /var/www/trading-bot
   php artisan schedule:list
   php artisan schedule:run -v
   ```
   `-v` покажет, какие команды выполняются.

5. **Логи cron (если есть):**  
   На разных системах: `/var/log/syslog`, `journalctl -u cron -f`, или в письмах при настройке MAILTO в crontab.

**Итог:** в первую очередь смотри `storage/logs/laravel.log` и запускай `php artisan bots:run` вручную — по ним обычно сразу видно причину (ошибка API, исключение в коде, неверный путь и т.д.).

---

## 📲 Уведомления в Telegram не приходят — как наладить

Боты при каждом запуске отправляют в Telegram: «ЗАПУСК БОТОВ», «ЗАПУСК ФЬЮЧЕРСНЫХ БОТОВ», «ЗАПУСК БОТОВ ЗА BTC» и уведомления о сделках (BUY/SELL). Если сообщения не доходят:

**1. Проверить отправку тестовым сообщением:**
```bash
cd /var/www/trading-bot
php artisan telegram:test
```
- Если выведет «Telegram not configured» — в `.env` не заданы или не подхвачены `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`.
- Если «Failed to send» — смотри пункты 2–4 и логи.

**2. Проверить .env:**
```bash
grep TELEGRAM /var/www/trading-bot/.env
```
Должны быть (без пробелов вокруг `=`):
- `TELEGRAM_BOT_TOKEN=123456:ABC...` — токен от @BotFather.
- `TELEGRAM_CHAT_ID=-1001234567890` — ID чата (личный или группы). Узнать: написать боту в Telegram команду /start, затем `php artisan telegram:chat-id`.

После правки `.env` выполнить:
```bash
php artisan config:clear
php artisan telegram:test
```

**3. Если включена очередь (`TELEGRAM_QUEUE=true`):**  
Сообщения попадают в таблицу `jobs` и отправляются только воркером. Без запущенного воркера уведомления не уйдут.
```bash
# Проверить очередь
grep TELEGRAM_QUEUE /var/www/trading-bot/.env

# Если true — запустить воркер (в отдельном терминале или через Supervisor)
php artisan queue:work
```
Либо поставить `TELEGRAM_QUEUE=false` в `.env`, тогда отправка идёт сразу из команд (без воркера).

**4. Посмотреть логи на ошибки Telegram:**
```bash
grep -i telegram /var/www/trading-bot/storage/logs/laravel.log | tail -20
```
Типичные записи: «Telegram not configured: bot_token or chat_id missing», «Telegram API error» с кодом (например 400 — неверный chat_id, 401 — неверный токен).

**5. Heartbeat (если используешь отдельный чат):**  
Для «сервер жив» используются `TELEGRAM_HEALTH_BOT_TOKEN` и `TELEGRAM_HEALTH_CHAT_ID`. Если нужны только основные уведомления — достаточно основного бота и `TELEGRAM_CHAT_ID`.

**Итог:** выполни `php artisan config:clear`, затем `php artisan telegram:test`. Если тест прошёл — уведомления от ботов должны идти; при `TELEGRAM_QUEUE=true` обязательно должен работать `queue:work`.

---

## 📊 Где искать логи покупок и продаж:

### 1. Laravel логи (основные логи приложения):

**Путь:**
```bash
/var/www/trading-bot/storage/logs/laravel.log
```

**Как посмотреть:**
```bash
# Весь лог
cat /var/www/trading-bot/storage/logs/laravel.log

# Последние 100 строк
tail -n 100 /var/www/trading-bot/storage/logs/laravel.log

# Мониторинг в реальном времени
tail -f /var/www/trading-bot/storage/logs/laravel.log

# Фильтр по ключевым словам
grep "BUY\|SELL" /var/www/trading-bot/storage/logs/laravel.log
```

**Что там есть:**
- Все действия ботов (BUY/SELL/HOLD)
- Уведомления о сделках
- Ошибки и предупреждения
- Информация о синхронизации ордеров

---

### 2. База данных (самый точный источник):

**Таблица `trades`:**
```bash
php artisan tinker
```

```php
// Все сделки
\App\Models\Trade::all();

// Последние 20 сделок
\App\Models\Trade::latest()->take(20)->get();

// Только BUY сделки
\App\Models\Trade::where('side', 'BUY')->latest()->get();

// Только SELL сделки
\App\Models\Trade::where('side', 'SELL')->latest()->get();

// Только исполненные (FILLED)
\App\Models\Trade::where('status', 'FILLED')->latest()->get();

// Сделки конкретного бота
$bot = \App\Models\TradingBot::find(1);
$bot->trades()->latest()->get();

exit
```

**SQL запрос (если есть прямой доступ к БД):**
```sql
-- Все сделки
SELECT * FROM trades ORDER BY created_at DESC;

-- Последние 20 сделок
SELECT * FROM trades ORDER BY created_at DESC LIMIT 20;

-- Только BUY
SELECT * FROM trades WHERE side = 'BUY' ORDER BY created_at DESC;

-- Только SELL
SELECT * FROM trades WHERE side = 'SELL' ORDER BY created_at DESC;

-- Только исполненные
SELECT * FROM trades WHERE status = 'FILLED' ORDER BY created_at DESC;

-- Статистика по боту
SELECT 
    side,
    status,
    COUNT(*) as count,
    SUM(quantity * price) as total_value,
    SUM(realized_pnl) as total_pnl
FROM trades 
WHERE trading_bot_id = 1
GROUP BY side, status;
```

---

### 3. Telegram уведомления:

**Где смотреть:**
- Telegram чат с ботом
- Все уведомления о BUY/SELL/HOLD
- Уведомления о исполненных ордерах (FILLED)
- Уведомления об ошибках

**Что отслеживать:**
- Каждое уведомление содержит:
  - Тип действия (BUY/SELL/HOLD)
  - Символ (Symbol)
  - Количество (Quantity)
  - Цена (Price)
  - Время (Time)

---

### 4. Dashboard (веб-интерфейс):

**URL:**
```
http://ваш-сервер/dashboard
```

**Что показывает:**
- Список всех ботов
- Статистика по ботам
- Открытые позиции
- Последние сделки
- PnL (прибыль/убыток)

---

### 5. Cron логи (если настроены):

**Если логирование cron настроено:**
```bash
# Проверить логи cron (если есть)
tail -f /var/log/cron

# Или через journalctl (systemd)
journalctl -u cron -f
```

---

## 🔍 Поиск конкретной информации:

### Все покупки:

```bash
# В логах
grep "BUY order" /var/www/trading-bot/storage/logs/laravel.log

# В БД через tinker
php artisan tinker
>>> \App\Models\Trade::where('side', 'BUY')->latest()->get();
```

### Все продажи:

```bash
# В логах
grep "SELL order\|SELL.*EXECUTING" /var/www/trading-bot/storage/logs/laravel.log

# В БД через tinker
php artisan tinker
>>> \App\Models\Trade::where('side', 'SELL')->latest()->get();
```

### Ошибки:

```bash
# В логах
grep -i "error\|exception\|failed" /var/www/trading-bot/storage/logs/laravel.log

# Последние ошибки
tail -n 500 /var/www/trading-bot/storage/logs/laravel.log | grep -i error
```

### Статистика за период:

```php
// В tinker
php artisan tinker

// Статистика за последние 24 часа
$since = now()->subDay();
\App\Models\Trade::where('created_at', '>=', $since)
    ->where('status', 'FILLED')
    ->get();

// Статистика по PnL
$pnl = \App\Models\Trade::whereNotNull('realized_pnl')->sum('realized_pnl');
echo "Total PnL: {$pnl} USDT\n";

exit
```

---

## 📋 Рекомендации по мониторингу:

### Ежедневный мониторинг:

1. **Telegram** → проверять уведомления
2. **Dashboard** → проверять статистику
3. **Логи** → проверять только при проблемах

### При проблемах:

1. **Логи** → детальная информация об ошибках
2. **БД** → точная информация о сделках
3. **Telegram** → уведомления об ошибках

### Для анализа:

1. **БД** → самая точная информация
2. **Dashboard** → визуальное представление
3. **Логи** → детальная история действий

---

## 🎯 Главные места для проверки:

1. **Telegram** → уведомления в реальном времени
2. **`/var/www/trading-bot/storage/logs/laravel.log`** → детальные логи
3. **База данных (таблица `trades`)** → точная информация о сделках
4. **Dashboard** → визуальная статистика

---

## 📝 Примеры команд для сервера:

```bash
# Подключиться к серверу
ssh root@ваш-сервер

# Перейти в директорию проекта
cd /var/www/trading-bot

# Мониторинг логов в реальном времени
tail -f storage/logs/laravel.log

# Последние сделки через tinker
php artisan tinker
>>> \App\Models\Trade::latest()->take(10)->get();
>>> exit

# Проверить статус ботов
php artisan tinker
>>> \App\Models\TradingBot::where('is_active', true)->get();
>>> exit
```

---

## ⚠️ Важно:

- **Логи** могут быть большими - используйте фильтры
- **БД** - самый надежный источник для анализа
- **Telegram** - для мониторинга в реальном времени
- **Dashboard** - для визуального обзора
