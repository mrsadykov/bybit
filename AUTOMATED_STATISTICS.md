# 📊 Автоматическая статистика в дашборде

## ✅ Что сделано

### 1. Таблица `bot_statistics` для хранения статистики

Создана таблица для сохранения результатов анализа производительности:
- Статистика по каждому боту
- Общая статистика (без привязки к боту)
- Автоматическое обновление каждый день

### 2. Команда `stats:analyze` сохраняет результаты в БД

Теперь команда автоматически сохраняет результаты анализа в базу данных при каждом запуске.

### 3. Добавлено в cron

Команда `stats:analyze --days=30` запускается **каждый день в 00:00** автоматически.

### 4. Отображение в веб-дашборде

На главной странице `/dashboard` теперь отображается:
- **Сохраненная статистика за 30 дней** (обновляется автоматически)
- Win Rate, Profit Factor, количество сделок, средний PnL
- Время последнего обновления

---

## 🎯 Как это работает

### Автоматический процесс:

1. **Каждый день в 00:00** cron запускает `stats:analyze --days=30`
2. Команда анализирует все боты за последние 30 дней
3. Результаты сохраняются в таблицу `bot_statistics`
4. На дашборде отображается последняя сохраненная статистика

### Ручной запуск:

```bash
# Анализ и сохранение в БД
php artisan stats:analyze --days=30

# Анализ конкретного бота
php artisan stats:analyze --bot=1 --days=30

# Экспорт в CSV (без сохранения в БД)
php artisan stats:analyze --export=stats.csv
```

---

## 📊 Что отображается в дашборде

### Блок "Сохраненная статистика за 30 дней":

- **Win Rate** - процент прибыльных сделок
- **Profit Factor** - соотношение прибыли к убыткам
  - 🟢 ≥ 1.5 = Отлично
  - 🟡 ≥ 1.0 = Хорошо
  - 🔴 < 1.0 = Требует внимания
- **Сделок** - общее количество закрытых позиций
- **Средний PnL** - средняя прибыль на сделку

Также показывается время последнего обновления.

---

## 🔍 Просмотр истории статистики

### Через базу данных:

```sql
-- Последняя статистика по всем ботам
SELECT * FROM bot_statistics 
WHERE trading_bot_id IS NULL 
ORDER BY analysis_date DESC 
LIMIT 1;

-- Статистика конкретного бота
SELECT * FROM bot_statistics 
WHERE trading_bot_id = 1 
ORDER BY analysis_date DESC 
LIMIT 10;
```

### Через команду:

```bash
# Анализ за другой период (не сохраняется в БД)
php artisan stats:analyze --days=7

# Анализ за месяц
php artisan stats:analyze --days=30
```

---

## ⚙️ Настройка cron

Команда уже добавлена в `routes/console.php`:

```php
// Анализ производительности каждый день в 00:00
Schedule::command('stats:analyze --days=30')->dailyAt('00:00')->withoutOverlapping();
```

Если нужно изменить время или период:

```php
// Каждый день в 03:00
Schedule::command('stats:analyze --days=30')->dailyAt('03:00')->withoutOverlapping();

// Каждую неделю
Schedule::command('stats:analyze --days=7')->weekly();
```

---

## 📈 Преимущества

1. **Автоматизация** - не нужно вручную запускать анализ
2. **История** - статистика сохраняется каждый день
3. **Быстрый доступ** - данные сразу видны в дашборде
4. **Сравнение** - можно отслеживать изменения во времени

---

## 🚀 Следующие шаги

1. **Дождитесь первого автоматического запуска** (00:00)
2. **Проверьте дашборд** - должна появиться статистика
3. **Сравнивайте результаты** - отслеживайте изменения

---

**Готово!** Теперь статистика автоматически собирается и отображается в дашборде! 🎉
