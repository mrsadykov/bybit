# Логичная последовательность создания работающего торгового бота

## Текущее состояние проекта

### ✅ Что уже есть:
- ✅ Архитектура бота готова
- ✅ Стратегия RSI+EMA реализована
- ✅ Индикаторы (RSI, EMA) работают
- ✅ Подключение к биржам (Bybit, OKX)
- ✅ Команды для управления (создание ботов, запуск)
- ✅ Синхронизация ордеров
- ✅ Позиционный менеджер

### ❓ Что нужно проверить/добавить:
- Проверить есть ли уже созданные боты
- Решить на какой бирже работать (Bybit или OKX)
- Протестировать полный цикл торговли

## Логичная последовательность действий

### Этап 1: Подготовка (5-10 минут)

#### 1.1. Выбрать биржу
**Вопрос:** На какой бирже работать?

- **OKX** - рекомендую (история 3 месяца, вывод работает для россиян)
- **Bybit** - если уже есть средства там

#### 1.2. Проверить есть ли боты
```bash
php artisan tinker
# В tinker:
\App\Models\TradingBot::with('exchangeAccount')->get()->each(fn($b) => print("Bot #{$b->id}: {$b->symbol}, active: " . ($b->is_active ? 'yes' : 'no') . ", exchange: " . ($b->exchangeAccount->exchange ?? 'none') . PHP_EOL));
```

#### 1.3. Создать торгового бота (если еще нет)
```bash
# Для Bybit:
php artisan bybit-bot:create BTCUSDT 5m rsi_ema 10

# Для OKX (нужно будет создать команду или использовать существующую):
php artisan bybit-bot:create BTCUSDT 5m rsi_ema 10
# Затем в БД изменить exchange_account_id на OKX аккаунт
```

### Этап 2: Тестирование стратегии (без реальных сделок)

#### 2.1. Проверить работу стратегии (dry run)
```bash
# Запустить бота в режиме dry_run (без реальных сделок)
# Или проверить что стратегия генерирует сигналы
php artisan bots:run
```

**Что проверить:**
- ✅ Получает ли цену
- ✅ Получает ли свечи
- ✅ Считает ли индикаторы (RSI, EMA)
- ✅ Генерирует ли сигналы (BUY/SELL/HOLD)
- ✅ Правильно ли определяет позицию

### Этап 3: Тестовая покупка (если все выше работает)

#### 3.1. Убедиться что есть средства
```bash
# Проверить баланс
php artisan balance:check USDT --exchange=okx  # или bybit
```

#### 3.2. Сделать тестовую покупку (маленькая сумма)
- Включить бота (`is_active = true`)
- Установить маленький `position_size` (например 5-10 USDT)
- Запустить `php artisan bots:run`
- Дождаться BUY сигнала
- Проверить что ордер создан и исполнен

**Проверить:**
- ✅ Ордер создан в БД
- ✅ Ордер отправлен на биржу
- ✅ Ордер исполнен (через `php artisan orders:sync`)
- ✅ Баланс изменился

### Этап 4: Тестовая продажа

#### 4.1. Дождаться SELL сигнала
- Бот должен закрыть позицию (продать)
- Проверить что SELL ордер создан и исполнен

**Проверить:**
- ✅ SELL ордер создан
- ✅ Позиция закрыта
- ✅ PnL рассчитан
- ✅ Баланс обновился

### Этап 5: Полный цикл

#### 5.1. Автоматизация
```bash
# Настроить cron для автоматического запуска
# Каждые 5 минут:
*/5 * * * * cd /path/to/project && php artisan bots:run
# Каждую минуту (для синхронизации ордеров):
* * * * * cd /path/to/project && php artisan orders:sync
```

## Рекомендуемая последовательность для ВАС

### 🟢 Вариант 1: Быстрый старт (рекомендую)

1. **Проверить текущее состояние** (1 минута)
   ```bash
   php artisan tinker
   # Посмотреть есть ли боты
   ```

2. **Создать бота на OKX** (2 минуты)
   - Использовать существующую команду (нужно будет адаптировать)
   - Или создать нового бота через команду

3. **Тестовая покупка с минимальной суммой** (5 минут)
   - Установить `position_size = 5 USDT` (минимальная сумма)
   - Включить бота
   - Запустить `php artisan bots:run`
   - Проверить через `php artisan orders:sync`

4. **Тестовая продажа** (5 минут)
   - Дождаться SELL сигнала (или вызвать вручную)
   - Проверить что позиция закрыта

5. **Автоматизация** (2 минуты)
   - Настроить cron

**Время:** ~15 минут

### 🟡 Вариант 2: Полное тестирование

1. **Проверить стратегию** (dry run)
2. **Проверить индикаторы**
3. **Проверить сигналы**
4. **Тестовая покупка**
5. **Тестовая продажа**
6. **Автоматизация**

**Время:** ~30 минут

## Что делать СЕЙЧАС?

### Рекомендую начать с:

1. **Проверить текущее состояние** (есть ли боты)
2. **Создать бота на OKX** (если еще нет)
3. **Сделать тестовую покупку** (маленькая сумма: 5-10 USDT)
4. **Проверить что все работает**
5. **Дождаться SELL сигнала или сделать тестовую продажу**

### Важные моменты:

⚠️ **Начните с маленькой суммы** (5-10 USDT) для тестирования
⚠️ **Используйте OKX** (история 3 месяца, вывод работает)
⚠️ **Тестируйте на реальных средствах** (но маленьких) для проверки всех механизмов
⚠️ **Настройте cron** для автоматического запуска

## Проверка пунктов улучшений (чек-лист)

### 3. Фильтр тренда или объёма (с допуском) — как проверить, что сделано

Фильтры включаются в **.env**. Проверка:

**Вариант А — через .env:**
- Откройте `.env` и найдите:
  - **Тренд:** `TRADING_TREND_FILTER_ENABLED=true` и желательно `TRADING_TREND_FILTER_TOLERANCE_PERCENT=0.5` (допуск 0.5% ниже длинной EMA).
  - **Объём:** `TRADING_VOLUME_FILTER_ENABLED=true` и желательно `TRADING_VOLUME_FILTER_MIN_RATIO=0.8` (вход при объёме ≥ 80% от среднего).
- Должен быть включён **только один** из двух (тренд **или** объём), не оба сразу — иначе на истории может быть 0 сделок.

**Вариант Б — через tinker:**
```bash
php artisan tinker
>>> config('trading.trend_filter_enabled')
=> true   # или false
>>> config('trading.trend_filter_tolerance_percent')
=> 0.5    # допуск в %
>>> config('trading.volume_filter_enabled')
=> false
>>> exit
```
Если `trend_filter_enabled` = true и `trend_filter_tolerance_percent` > 0 (например 0.5) — пункт «один фильтр с допуском» выполнен. Аналогично для объёма: `volume_filter_enabled` = true и `volume_filter_min_ratio` < 1.0 (например 0.8).

**Итог:** пункт сделан, если в .env есть либо тренд (с допуском), либо объём (с допуском), и после `php artisan config:clear` значения видны в `config('trading.*')`.

---

### 5. position_size_multiplier — как сделать

Сейчас множитель берётся из **.env** (по умолчанию 1, если не задан).

**Добавить в .env:**
```env
# Множитель размера позиции: 0.5 = половинный размер по всем ботам (спот, фьючерсы, btc-quote)
TRADING_POSITION_SIZE_MULTIPLIER=0.5
```
Значения: `0.5` — осторожно (половинный размер), `0.7`–`1.0` — при уверенности в результатах. Не ставить больше 1 без понимания риска.

**Проверка:**
```bash
php artisan config:clear
php artisan tinker
>>> config('trading.position_size_multiplier')
=> 0.5
>>> exit
```

---

### 7. Таймфрейм 4h (по желанию)

Не обязательно. Если делать: в дашборде создать **нового** спот-бота с таймфреймом **4h** по одной паре (например ETHUSDT или BNBUSDT), те же RSI/EMA/SL/TP. Через 1–2 недели сравнить число сделок и PnL с ботом на 1h.

---

## Вопросы для уточнения:

1. Есть ли уже созданные боты?
2. На какой бирже хотите работать? (OKX или Bybit)
3. Сколько готовы потратить на тестирование? (рекомендую 5-10 USDT)
