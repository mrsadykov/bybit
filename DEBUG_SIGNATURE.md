# 🔍 Диагностика проблемы с подписью Bybit API

## ✅ Что работает
- Публичный API работает (цена получается)
- API Secret расшифровывается (36 символов)
- API Key правильный (18 символов)

## ❌ Проблема
- Приватный API возвращает 401 (Unauthorized)
- Это означает проблему с подписью или правами

## 🔍 Возможные причины

### 1. API ключ не имеет прав
- Проверьте на bybit.com что ключ имеет **Read** permission
- Убедитесь что права не были отозваны

### 2. IP Whitelist
- Если включен IP whitelist - добавьте ваш IP
- Или отключите для тестирования

### 3. Неправильный API Secret
- Убедитесь что secret скопирован полностью
- Нет лишних пробелов в начале/конце
- Правильный формат (обычно 36 символов)

### 4. Проблема с подписью
- Проверьте что timestamp правильный
- Проверьте формат query string

## 🛠️ Проверка

### 1. Проверьте права API ключа

На bybit.com:
1. API Management → ваш ключ
2. Проверьте что есть **Read** permission
3. Проверьте IP whitelist (если включен)

### 2. Проверьте формат ключей

```bash
php artisan tinker
```

```php
>>> $account = \App\Models\ExchangeAccount::find(8);
>>> echo "Key: " . $account->api_key . "\n";
>>> echo "Key length: " . strlen($account->api_key) . "\n";
>>> echo "Secret length: " . strlen($account->api_secret) . "\n";
>>> echo "Secret (first 10): " . substr($account->api_secret, 0, 10) . "\n";
>>> exit
```

### 3. Попробуйте создать новый API ключ

Если старый не работает:
1. Создайте новый на bybit.com
2. Обновите в .env
3. Запустите `php artisan create-bybit-account`

## 💡 Быстрое решение

1. **Проверьте права ключа** на bybit.com
2. **Проверьте IP whitelist** (отключите для теста)
3. **Создайте новый ключ** если старый не работает
4. **Обновите аккаунт**: `php artisan create-bybit-account`
