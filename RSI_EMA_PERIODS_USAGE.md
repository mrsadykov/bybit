# 📊 Использование настраиваемых периодов RSI/EMA

## ✅ Реализовано

Теперь каждый бот может иметь свои собственные периоды RSI и EMA, хранящиеся в БД.

---

## 🚀 Применение миграции

```bash
php artisan migrate
```

Это добавит поля `rsi_period` и `ema_period` в таблицу `trading_bots`.

---

## 📝 Создание бота с настраиваемыми периодами

### OKX бот:

```bash
# С дефолтными периодами (RSI=17, EMA=10)
php artisan okx-bot:create SOLUSDT 1h rsi_ema 10

# С кастомными периодами для SOLUSDT (рекомендуется)
php artisan okx-bot:create SOLUSDT 1h rsi_ema 10 \
  --rsi-period=17 \
  --ema-period=15

# Или EMA=20 для более медленной реакции
php artisan okx-bot:create SOLUSDT 1h rsi_ema 10 \
  --rsi-period=17 \
  --ema-period=20
```

### Bybit бот:

```bash
# С дефолтными периодами
php artisan bybit-bot:create BTCUSDT 1h rsi_ema 10

# С кастомными периодами
php artisan bybit-bot:create BTCUSDT 1h rsi_ema 10 \
  --rsi-period=17 \
  --ema-period=10
```

---

## 🔧 Установка периодов для существующих ботов

### Через tinker:

```bash
php artisan tinker
```

```php
// Для SOLUSDT бота (Bot #3) - рекомендуется EMA=15-20
$bot = \App\Models\TradingBot::find(3);
$bot->rsi_period = 17;
$bot->ema_period = 15; // или 20
$bot->save();

// Для BTCUSDT бота (Bot #1) - дефолтные значения
$bot = \App\Models\TradingBot::find(1);
$bot->rsi_period = 17;
$bot->ema_period = 10;
$bot->save();

// Для ETHUSDT бота (Bot #2)
$bot = \App\Models\TradingBot::find(2);
$bot->rsi_period = 17;
$bot->ema_period = 10; // или 15
$bot->save();
```

### Через SQL:

```sql
-- Установить EMA=15 для SOLUSDT бота
UPDATE trading_bots 
SET ema_period = 15, rsi_period = 17 
WHERE symbol = 'SOLUSDT';

-- Установить дефолтные значения для всех ботов без периодов
UPDATE trading_bots 
SET rsi_period = 17, ema_period = 10 
WHERE rsi_period IS NULL OR ema_period IS NULL;
```

---

## 📊 Рекомендуемые значения

### BTCUSDT:
- **RSI период:** 17
- **EMA период:** 10
- **Таймфрейм:** 1h

### ETHUSDT:
- **RSI период:** 17
- **EMA период:** 10-15
- **Таймфрейм:** 1h

### SOLUSDT:
- **RSI период:** 17-20
- **EMA период:** 15-20 (рекомендуется 15)
- **Таймфрейм:** 1h или 4h

---

## 🔍 Как это работает

1. **При создании бота:**
   - Если указаны `--rsi-period` и `--ema-period` → сохраняются в БД
   - Если не указаны → `NULL` в БД

2. **При запуске бота (`bots:run`):**
   - Читает `rsi_period` и `ema_period` из БД
   - Если `NULL` → использует значения по умолчанию (RSI=17, EMA=10)
   - Передает параметры в `RsiEmaStrategy::decide()`

3. **В стратегии:**
   - `RsiEmaStrategy::decide($closes, $rsiPeriod, $emaPeriod)`
   - Использует переданные периоды для расчета индикаторов

---

## ✅ Проверка текущих значений

### Через tinker:

```bash
php artisan tinker
```

```php
\App\Models\TradingBot::all()->each(function($bot) {
    $rsi = $bot->rsi_period ?? 17;
    $ema = $bot->ema_period ?? 10;
    echo "Bot #{$bot->id} ({$bot->symbol}): RSI={$rsi}, EMA={$ema}\n";
});
```

### Через SQL:

```sql
SELECT id, symbol, rsi_period, ema_period 
FROM trading_bots;
```

---

## 🎯 Примеры использования

### Пример 1: Создать SOLUSDT бот с оптимальными параметрами

```bash
php artisan okx-bot:create SOLUSDT 1h rsi_ema 10 \
  --rsi-period=17 \
  --ema-period=15 \
  --stop-loss=5.0 \
  --take-profit=12.0
```

### Пример 2: Обновить существующий SOLUSDT бот

```bash
php artisan tinker
```

```php
$bot = \App\Models\TradingBot::where('symbol', 'SOLUSDT')->first();
$bot->rsi_period = 17;
$bot->ema_period = 15;
$bot->save();
echo "✅ Обновлено: Bot #{$bot->id}\n";
```

### Пример 3: Установить одинаковые периоды для всех ботов

```bash
php artisan tinker
```

```php
\App\Models\TradingBot::whereNull('rsi_period')->update(['rsi_period' => 17]);
\App\Models\TradingBot::whereNull('ema_period')->update(['ema_period' => 10]);
echo "✅ Установлены дефолтные значения\n";
```

---

## ⚠️ Важно

1. **Обратная совместимость:**
   - Если периоды не указаны → используются дефолтные (RSI=17, EMA=10)
   - Старые боты продолжат работать без изменений

2. **Валидация:**
   - RSI период: 2-100
   - EMA период: 2-200

3. **После изменения периодов:**
   - Бот автоматически использует новые значения при следующем запуске
   - Не требуется перезапуск или пересоздание бота

---

## 📋 Чеклист после миграции

- [ ] Применить миграцию: `php artisan migrate`
- [ ] Установить периоды для существующих ботов (особенно SOLUSDT)
- [ ] Проверить значения через tinker или SQL
- [ ] Запустить бота и проверить, что используются правильные периоды
- [ ] Проверить логи: должны показывать периоды в выводе (например: "RSI (17): 50.73")

---

## 🎉 Готово!

Теперь каждый бот может иметь свои оптимальные периоды RSI и EMA! 🚀
