# 🎯 Стратегия оптимизации параметров (Strategy Optimization Guide)

## 📋 Что такое `strategy:optimize`?

Команда `strategy:optimize` **автоматически перебирает различные параметры** торговой стратегии RSI+EMA и находит **оптимальные значения** для максимальной прибыли.

---

## 🎯 Для чего нужна оптимизация?

### Проблема:
Текущие параметры стратегии могут быть **неоптимальными**:
- RSI период = 14 (стандарт, но может не подходить для вашего рынка)
- EMA период = 20 (стандарт, но может быть слишком быстрым или медленным)
- Stop-Loss = 5%, Take-Profit = 10% (может быть слишком консервативным или агрессивным)

### Решение:
**Автоматический перебор параметров** позволяет найти:
- ✅ **Лучшие параметры RSI** (10, 14, 21, 28...)
- ✅ **Лучшие параметры EMA** (10, 20, 30, 50...)
- ✅ **Оптимальные SL/TP** (3%/6%, 5%/10%, 7%/14%...)
- ✅ **Максимальный PnL** при минимальном риске

---

## 📊 Как работает оптимизация?

1. **Вы указываете диапазоны параметров:**
   - RSI: 10-28 (шаг 7) → тестирует 10, 17, 24
   - EMA: 10-50 (шаг 10) → тестирует 10, 20, 30, 40, 50
   - SL: 3, 5, 7
   - TP: 6, 10, 14

2. **Система перебирает все комбинации:**
   - RSI=10, EMA=10, SL=3, TP=6
   - RSI=10, EMA=10, SL=3, TP=10
   - RSI=10, EMA=10, SL=3, TP=14
   - ... и так далее для всех комбинаций

3. **Запускается бэктестинг** для каждой комбинации на исторических данных

4. **Выбираются лучшие параметры** по критерию (PnL, win rate, количество сделок)

---

## 🚀 Как использовать?

### Базовый вариант (с параметрами по умолчанию):

```bash
php artisan strategy:optimize BTCUSDT
```

**Что делает:**
- Тестирует RSI: 10, 17, 24 (шаг 7)
- Тестирует EMA: 10, 20, 30, 40, 50 (шаг 10)
- Тестирует SL: 3%, 5%, 7%
- Тестирует TP: 6%, 10%, 14%
- **Итого:** 3 × 5 × 3 × 3 = **135 комбинаций**

### Расширенный вариант (с настройкой параметров):

```bash
php artisan strategy:optimize BTCUSDT \
  --period=500 \
  --rsi-min=10 --rsi-max=28 --rsi-step=7 \
  --ema-min=10 --ema-max=50 --ema-step=10 \
  --sl-values=3,5,7 \
  --tp-values=6,10,14 \
  --sort-by=pnl
```

### Параметры команды:

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `symbol` | Торговая пара (BTCUSDT, ETHUSDT...) | - |
| `--timeframe` | Таймфрейм (5m, 15m, 1h, 1D) | 5m |
| `--exchange` | Биржа (okx, bybit) | okx |
| `--period` | Количество свечей для анализа | 500 |
| `--rsi-min` | Минимальный период RSI | 10 |
| `--rsi-max` | Максимальный период RSI | 28 |
| `--rsi-step` | Шаг для периода RSI | 7 |
| `--ema-min` | Минимальный период EMA | 10 |
| `--ema-max` | Максимальный период EMA | 50 |
| `--ema-step` | Шаг для периода EMA | 10 |
| `--sl-values` | Значения Stop-Loss (через запятую) | 3,5,7 |
| `--tp-values` | Значения Take-Profit (через запятую) | 6,10,14 |
| `--position-size` | Размер позиции в USDT | 100 |
| `--fee` | Комиссия (0.001 = 0.1%) | 0.001 |
| `--sort-by` | Сортировка (pnl, win_rate, trades) | pnl |

---

## 📈 Пример вывода:

```
ТОП-10 РЕЗУЛЬТАТОВ ОПТИМИЗАЦИИ (TOP-10 OPTIMIZATION RESULTS)
====================================================================================================

RSI    | EMA   | SL      | TP      | PnL (USDT)  | Return (%)   | Win Rate (%) | Trades
----------------------------------------------------------------------------------------------------
24     | 30    | 5       | 10      | 125.50      | 12.55        | 58.33        | 24
17     | 20    | 3       | 6       | 98.20       | 9.82         | 61.11        | 36
10     | 40    | 7       | 14      | 87.30       | 8.73         | 55.00        | 20
...

Лучшие параметры (Best Parameters):
  RSI период (RSI Period): 24
  EMA период (EMA Period): 30
  Stop-Loss: 5%
  Take-Profit: 10%
  Общий PnL (Total PnL): 125.50 USDT
  Доходность (Return): 12.55%
  Win Rate: 58.33%
  Сделок (Trades): 24
```

---

## 💡 Как использовать результаты?

### 1. Создайте бота с оптимальными параметрами:

```bash
php artisan okx-bot:create BTCUSDT 5m rsi_ema 100 \
  --stop-loss=5 \
  --take-profit=10
```

### 2. Или обновите существующего бота:

```bash
php artisan tinker
>>> $bot = \App\Models\TradingBot::find(24);
>>> $bot->stop_loss_percent = 5.0;
>>> $bot->take_profit_percent = 10.0;
>>> $bot->save();
>>> exit
```

### 3. Или вручную через dashboard/БД:

- Зайдите в dashboard
- Найдите нужного бота
- Обновите параметры RSI/EMA/SL/TP

**Примечание:** RSI и EMA параметры хранятся в самой стратегии, а не в базе данных. Для их изменения нужно обновить код стратегии.

---

## ⚠️ Важные замечания:

### 1. Оптимизация на исторических данных
- Параметры, которые работали в прошлом, **не гарантируют** успех в будущем
- Используйте оптимизацию как **рекомендацию**, а не как абсолютную истину

### 2. Переоптимизация (Overfitting)
- Слишком много параметров → **переоптимизация** → плохая работа на новых данных
- Рекомендуется: **широкий диапазон параметров**, но не слишком детальный

### 3. Время выполнения
- **135 комбинаций** могут занять **5-15 минут** в зависимости от:
  - Количества свечей (period)
  - Скорости интернета
  - Загрузки сервера

### 4. Рекомендуемые настройки для начала:

```bash
# Быстрая оптимизация (мало комбинаций)
php artisan strategy:optimize BTCUSDT \
  --period=200 \
  --rsi-min=14 --rsi-max=21 --rsi-step=7 \
  --ema-min=20 --ema-max=30 --ema-step=10 \
  --sl-values=5 \
  --tp-values=10

# Детальная оптимизация (много комбинаций)
php artisan strategy:optimize BTCUSDT \
  --period=1000 \
  --rsi-min=10 --rsi-max=28 --rsi-step=3 \
  --ema-min=10 --ema-max=50 --ema-step=5 \
  --sl-values=3,5,7,10 \
  --tp-values=6,10,14,20
```

---

## 🔄 Рабочий процесс:

### Шаг 1: Запустить оптимизацию
```bash
php artisan strategy:optimize BTCUSDT --period=500
```

### Шаг 2: Проанализировать результаты
- Посмотреть топ-10 комбинаций
- Выбрать параметры с хорошим балансом PnL/win rate

### Шаг 3: Протестировать на практике
- Создать бота с оптимальными параметрами
- Запустить в тестовом режиме (dry_run)
- Проверить работу на реальных данных

### Шаг 4: Применить на практике
- Если результаты хорошие → активировать бота
- Если нет → повторить оптимизацию с другими параметрами

---

## 📊 Примеры использования:

### Пример 1: Быстрый тест параметров для BTCUSDT
```bash
php artisan strategy:optimize BTCUSDT --period=300
```

### Пример 2: Детальная оптимизация для ETHUSDT
```bash
php artisan strategy:optimize ETHUSDT \
  --period=1000 \
  --timeframe=15m \
  --rsi-min=10 --rsi-max=28 --rsi-step=3 \
  --ema-min=10 --ema-max=50 --ema-step=5 \
  --sl-values=3,5,7,10 \
  --tp-values=6,10,14,20 \
  --sort-by=win_rate
```

### Пример 3: Оптимизация без SL/TP
```bash
php artisan strategy:optimize BTCUSDT \
  --sl-values=0 \
  --tp-values=0
```

---

## 🎯 Итог:

**`strategy:optimize`** - это **инструмент для поиска оптимальных параметров** торговой стратегии, который:

1. ✅ **Автоматически тестирует** множество комбинаций параметров
2. ✅ **Находит лучшие параметры** для максимального PnL
3. ✅ **Экономит время** - не нужно вручную перебирать параметры
4. ✅ **Дает рекомендации** по настройке ботов

**Используйте для:**
- Поиска оптимальных параметров перед созданием бота
- Улучшения производительности существующих ботов
- Понимания, какие параметры работают лучше на конкретном рынке
