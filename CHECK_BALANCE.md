# 💰 Где посмотреть тестовые монеты

## 🌐 На сайте Bybit Testnet

### Способ 1: Через веб-интерфейс

1. **Зайдите на**: https://testnet.bybit.com/
2. **Войдите** в свой аккаунт
3. **Перейдите в раздел "Assets"** или **"Wallet"** (кошелек)
4. **Выберите "Spot"** или **"Spot Account"**
5. Там вы увидите все ваши балансы, включая тестовые USDT

### Способ 2: Через торговый интерфейс

1. На https://testnet.bybit.com/
2. В правом верхнем углу нажмите на **баланс** или **"Assets"**
3. Выберите **"Spot"**
4. Увидите все монеты и их балансы

---

## 💻 Через ваш проект (Laravel)

### Способ 1: Через Tinker (простой способ)

```bash
php artisan tinker
```

```php
// Получить аккаунт
>>> $account = \App\Models\ExchangeAccount::first();

// Создать сервис Bybit
>>> $bybit = new \App\Services\Exchanges\Bybit\BybitService($account);

// Проверить баланс USDT
>>> $bybit->getBalance('USDT');

// Посмотреть все балансы
>>> $bybit->getAllBalances();

// Выход
>>> exit
```

### Способ 2: Через команду (быстрый способ)

Создайте временную команду или используйте скрипт проверки:

```bash
# Использовать скрипт проверки
./testnet-check.sh
```

Или создайте простую команду:

```bash
php artisan tinker --execute="
\$account = \App\Models\ExchangeAccount::first();
\$bybit = new \App\Services\Exchanges\Bybit\BybitService(\$account);
echo 'USDT Balance: ' . \$bybit->getBalance('USDT') . PHP_EOL;
echo 'All balances:' . PHP_EOL;
print_r(\$bybit->getAllBalances());
"
```

---

## 🔍 Детальная проверка

### Проверка через API напрямую

```bash
php artisan tinker
```

```php
// Получить аккаунт
>>> $account = \App\Models\ExchangeAccount::first();
>>> $account->is_testnet; // должно быть true

// Создать сервис
>>> $bybit = new \App\Services\Exchanges\Bybit\BybitService($account);

// Проверить что это testnet
>>> $bybit->getBalance('USDT'); // баланс USDT

// Все балансы
>>> $balances = $bybit->getAllBalances();
>>> foreach ($balances as $coin => $amount) {
    echo "$coin: $amount\n";
}

// Проверить цену (для проверки подключения)
>>> $bybit->getPrice('BTCUSDT');

>>> exit
```

---

## ⚠️ Если баланс не виден

### Проблема 1: Монеты еще не зачислены

**Решение:**
- Подождите несколько минут (обычно зачисляются сразу, но может быть задержка)
- Обновите страницу на testnet.bybit.com
- Проверьте email (может быть уведомление)

### Проблема 2: Неправильный аккаунт

**Решение:**
- Убедитесь что вы запросили монеты на том же аккаунте, где создали API ключи
- Проверьте что API ключи от testnet (не от production)

### Проблема 3: Монеты в другом аккаунте

**Решение:**
- На Bybit testnet может быть несколько типов аккаунтов:
  - **Spot Account** - для спотовой торговли
  - **Unified Account** - унифицированный аккаунт
- Убедитесь что монеты в **Spot Account**

### Проблема 4: API не видит баланс

**Проверьте:**
1. Правильность API ключей в `.env`
2. Права API ключа (должны быть Read + Trade)
3. Что ключи от testnet (не от production)

```bash
# Очистить кэш конфигурации
php artisan config:clear

# Проверить снова
php artisan tinker
>>> $account = \App\Models\ExchangeAccount::first();
>>> $bybit = new \App\Services\Exchanges\Bybit\BybitService($account);
>>> $bybit->getBalance('USDT');
```

---

## 📊 Полезные команды для проверки

### Быстрая проверка баланса

```bash
php artisan tinker --execute="
\$account = \App\Models\ExchangeAccount::first();
if (!\$account) {
    echo '❌ Аккаунт не найден. Запустите: php artisan setup\n';
    exit(1);
}

\$bybit = new \App\Services\Exchanges\Bybit\BybitService(\$account);
echo '💰 Баланс USDT: ' . \$bybit->getBalance('USDT') . '\n';
echo '\n📊 Все балансы:\n';
\$balances = \$bybit->getAllBalances();
foreach (\$balances as \$coin => \$amount) {
    if (\$amount > 0) {
        echo '  ' . \$coin . ': ' . \$amount . '\n';
    }
}
"
```

### Полная проверка подключения

```bash
./testnet-check.sh
```

---

## 🎯 Где обычно находятся монеты

На Bybit testnet монеты обычно находятся в:

1. **Spot Account** - основной аккаунт для спотовой торговли
2. **Wallet → Spot** - в разделе кошелька

Если вы запросили тестовые USDT, они должны быть в **Spot Account**.

---

## ✅ Чеклист проверки

- [ ] Зашли на https://testnet.bybit.com/
- [ ] В разделе "Assets" или "Wallet" видите баланс
- [ ] Баланс виден в Spot Account
- [ ] Через API (`$bybit->getBalance('USDT')`) тоже виден баланс
- [ ] Баланс достаточен для тестирования (минимум 1 USDT, лучше 5-10)

---

## 💡 Совет

Если монеты не видны через API, но видны на сайте:
1. Проверьте что API ключи правильные
2. Убедитесь что ключи имеют права на чтение баланса
3. Попробуйте создать новые API ключи с полными правами

Если монеты не видны нигде:
1. Подождите 5-10 минут
2. Попробуйте запросить монеты еще раз
3. Проверьте что вы на правильном сайте (testnet.bybit.com, не bybit.com)
