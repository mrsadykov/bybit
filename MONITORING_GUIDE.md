# 📊 Руководство по мониторингу торгового бота

## 🎯 Следующие шаги после запуска

### 1. Мониторинг в реальном времени

#### Telegram уведомления
Бот автоматически отправляет уведомления в Telegram:
- ✅ **Покупка/продажа**: Уведомления о выполненных ордерах
- ⏸️ **HOLD**: Сообщения "NO ACTION TAKEN" с RSI/EMA значениями
- 📈 **Закрытие позиций**: Уведомления о закрытых позициях с PnL
- 🚀 **Запуск ботов**: Уведомления о начале работы ботов
- ⚠️ **Ошибки**: Уведомления об ошибках API или логики

**Проверка:**
```bash
# Проверить, что Telegram бот работает
php artisan telegram:test
```

#### Ежедневная статистика
Автоматически отправляется каждый день в 09:00:
```bash
# Проверить команду вручную
php artisan telegram:daily-stats
```

**Что включает:**
- Количество активных ботов
- Общий баланс
- Открытые позиции
- Прибыль/убыток за день
- Статистика по каждому боту

---

### 2. Мониторинг через веб-интерфейс

#### Дашборд
- **URL**: `/dashboard`
- **Что показывает:**
  - Список всех ботов
  - Статус (активен/неактивен)
  - Торговая пара
  - Размер позиции
  - Stop-Loss / Take-Profit

#### Открытые позиции
- **URL**: `/dashboard` (секция "Открытые позиции")
- **Что показывает:**
  - Символ
  - Количество
  - Цена входа
  - Дата покупки
  - Бот

#### История сделок
- **URL**: `/dashboard` (секция "Последние сделки")
- **Что показывает:**
  - Дата
  - Сторона (BUY/SELL)
  - Символ
  - Количество
  - Цена
  - Статус
  - PnL

---

### 3. Мониторинг через команды

#### Проверка баланса
```bash
# Проверить баланс всех аккаунтов
php artisan balance:check

# Проверить конкретный аккаунт
php artisan balance:check --account=1

# Проверить только OKX
php artisan balance:check --exchange=okx
```

#### Проверка статуса ботов
```bash
# Запустить ботов вручную (dry-run)
php artisan bots:run

# Проверить статус ботов
php artisan bots:check
```

#### Синхронизация ордеров
```bash
# Синхронизировать статусы ордеров с биржи
php artisan orders:sync

# Восстановить пропущенные сделки
php artisan trades:recover
```

#### Проверка открытых позиций
```bash
# Закрыть маленькие позиции (< 1 USDT)
php artisan positions:close-small --dry-run

# Синхронизировать позиции из баланса
php artisan position:sync-from-balance
```

---

### 4. Логи на сервере

#### Расположение логов
```bash
# Основные логи Laravel
tail -f storage/logs/laravel-$(date +%Y-%m-%d).log

# Логи ошибок
tail -f storage/logs/laravel-$(date +%Y-%m-%d).log | grep ERROR

# Логи покупок/продаж
tail -f storage/logs/laravel-$(date +%Y-%m-%d).log | grep -E "(BUY|SELL|FILLED)"
```

#### Что искать в логах
- `Order execution update` - обновления статусов ордеров
- `Position closed` - закрытие позиций
- `Order sync error` - ошибки синхронизации
- `OKX API error` / `Bybit error` - ошибки API биржи

---

### 5. Мониторинг базы данных

#### Проверка открытых позиций
```sql
SELECT 
    t.id,
    t.symbol,
    t.side,
    t.quantity,
    t.price,
    t.filled_at,
    b.name as bot_name
FROM trades t
JOIN trading_bots b ON t.trading_bot_id = b.id
WHERE t.side = 'BUY' 
  AND t.status = 'FILLED'
  AND t.closed_at IS NULL
ORDER BY t.filled_at DESC;
```

#### Проверка PnL
```sql
SELECT 
    symbol,
    SUM(realized_pnl) as total_pnl,
    COUNT(*) as closed_trades
FROM trades
WHERE closed_at IS NOT NULL
  AND realized_pnl IS NOT NULL
GROUP BY symbol
ORDER BY total_pnl DESC;
```

#### Проверка последних сделок
```sql
SELECT 
    id,
    side,
    symbol,
    quantity,
    price,
    status,
    filled_at,
    realized_pnl
FROM trades
ORDER BY filled_at DESC
LIMIT 20;
```

---

### 6. Автоматический мониторинг (Cron)

#### Текущие задачи
Проверить `routes/console.php`:
```php
Schedule::command('bots:run')->everyFiveMinutes();
Schedule::command('orders:sync')->everyFiveMinutes();
Schedule::command('telegram:daily-stats')->dailyAt('09:00');
Schedule::command('positions:close-small')->dailyAt('03:00');
```

#### Проверка работы cron
```bash
# Проверить, что cron работает
crontab -l

# Проверить логи cron
grep CRON /var/log/syslog | tail -20
```

---

### 7. Метрики для отслеживания

#### Ключевые показатели
1. **Количество сделок в день**
   - Цель: 2-5 сделок на бота
   - Слишком много = возможна переторговля
   - Слишком мало = стратегия не активна

2. **Win Rate (процент прибыльных сделок)**
   - Цель: > 50%
   - Формула: `(прибыльные сделки / все сделки) * 100`

3. **Средний PnL на сделку**
   - Цель: положительный
   - Формула: `SUM(realized_pnl) / COUNT(closed_trades)`

4. **Максимальная просадка (Drawdown)**
   - Цель: < 10%
   - Формула: `(пик баланса - минимум баланса) / пик баланса`

5. **Соотношение прибыль/риск**
   - Цель: > 2:1
   - Формула: `средняя прибыль / средний убыток`

---

### 8. Алерты и уведомления

#### Настройка критических алертов
Можно добавить в `TelegramService`:
- ⚠️ **Большая просадка**: Если баланс упал на > 10%
- ⚠️ **Много ошибок**: Если > 5 ошибок за час
- ⚠️ **Нет сделок**: Если бот не торгует > 24 часа
- ⚠️ **Низкий баланс**: Если баланс < минимального размера позиции

---

### 9. Рекомендации по мониторингу

#### Ежедневно
- ✅ Проверить Telegram уведомления
- ✅ Проверить ежедневную статистику
- ✅ Проверить баланс на бирже
- ✅ Проверить открытые позиции в веб-интерфейсе

#### Еженедельно
- ✅ Проанализировать PnL по каждому боту
- ✅ Проверить логи на ошибки
- ✅ Сравнить результаты с оптимизацией стратегии
- ✅ Проверить работу всех ботов

#### Ежемесячно
- ✅ Полный анализ производительности
- ✅ Сравнение с предыдущим месяцем
- ✅ Решение о добавлении/удалении ботов
- ✅ Обновление параметров стратегии

---

### 10. Инструменты для расширенного мониторинга

#### Возможные улучшения
1. **Grafana + InfluxDB**
   - Визуализация метрик в реальном времени
   - Графики баланса, PnL, количества сделок

2. **Sentry / Bugsnag**
   - Автоматический мониторинг ошибок
   - Уведомления о критических проблемах

3. **Custom Dashboard**
   - Расширенный веб-дашборд с графиками
   - История всех сделок с фильтрами

4. **Email отчеты**
   - Еженедельные/месячные отчеты на email
   - Детальная статистика и анализ

---

## 🎯 Итоговый чеклист мониторинга

- [ ] Настроены Telegram уведомления
- [ ] Проверена ежедневная статистика
- [ ] Настроен мониторинг логов
- [ ] Проверена работа cron задач
- [ ] Настроен мониторинг баланса
- [ ] Регулярно проверяется веб-интерфейс
- [ ] Отслеживаются ключевые метрики
- [ ] Настроены алерты для критических ситуаций

---

## 📞 Что делать при проблемах

1. **Бот не торгует**
   - Проверить `php artisan bots:run`
   - Проверить логи на ошибки
   - Проверить баланс на бирже

2. **Позиции не закрываются**
   - Запустить `php artisan orders:sync`
   - Проверить `parent_id` в базе данных
   - Проверить логи на ошибки

3. **Ошибки API**
   - Проверить API ключи
   - Проверить права API ключей
   - Проверить лимиты API биржи

4. **Неправильный PnL**
   - Проверить связь BUY/SELL через `parent_id`
   - Запустить `php artisan trades:recover`
   - Проверить расчеты в логах
