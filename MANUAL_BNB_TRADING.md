# 🤖 Ручная торговля BNBUSDT для проверки

## 📋 Команды для тестирования

### 1. Покупка BNB (Dry Run)

```bash
# Покупка на 5 USDT (символ берется из бота #4)
php artisan trade:buy 5 --bot=4 --dry-run

# Покупка на 10 USDT (символ берется из бота #4)
php artisan trade:buy 10 --bot=4 --dry-run

# Можно указать символ явно (если нужно)
php artisan trade:buy 5 BNBUSDT --bot=4 --dry-run
```

### 2. Продажа BNB (Dry Run)

```bash
# Продать ВСЕ доступные BNB (символ берется из бота #4)
php artisan trade:sell --all --bot=4 --dry-run

# Продать конкретное количество (например, 0.01 BNB)
php artisan trade:sell 0.01 --bot=4 --dry-run

# Можно указать символ явно (если нужно)
php artisan trade:sell 0.01 BNBUSDT --bot=4 --dry-run
```

### 3. Реальная торговля (после проверки)

```bash
# Покупка (РЕАЛЬНАЯ ТОРГОВЛЯ) - символ из бота
php artisan trade:buy 5 --bot=4

# Продажа (РЕАЛЬНАЯ ТОРГОВЛЯ) - символ из бота
php artisan trade:sell --all --bot=4
```

## ⚠️ Важно

- **Всегда начинайте с `--dry-run`** для проверки
- Убедитесь, что бот #4 существует и активен
- Проверьте баланс перед реальной торговлей: `php artisan balance:check --exchange=okx`

## 📊 Проверка баланса

```bash
# Проверить баланс OKX
php artisan balance:check --exchange=okx

# Проверить конкретный аккаунт
php artisan balance:check --account=1
```

## ✅ После проверки

Если все работает в dry-run режиме, можно убрать `--dry-run` и выполнить реальную торговлю.
