# Что дальше? Что можно улучшить?

**Дата:** 2026-01-31  
**Текущее состояние:** Spot, фьючерсы и BTC-quote боты работают; дашборд объединяет метрики по всем типам.

---

## ✅ Уже сделано

- Spot-боты (OKX), фьючерсные боты (OKX), BTC-quote боты (OKX)
- Дашборд: общие метрики (боты, сделки, PnL, win rate, открытые позиции) по всем типам
- Графики за 30 дней: кумулятивный PnL, PnL по дням, сделки по дням — с учётом spot, futures, BTC-quote
- Таблица открытых позиций с типом (Spot / Фьючерсы / BTC-quote)
- Telegram: уведомления о запуске и сделках по всем типам ботов
- Риск-лимиты для spot: max_daily_loss, max_drawdown, max_open_positions_total
- Алерты: дневной убыток, серия убытков, целевая прибыль
- Расписание: `bots:run`, `futures:run`, `btc-quote:run` каждые 5 мин; `orders:sync`, heartbeat, daily stats

---

## 🔧 Быстрые улучшения (1–2 дня)

### 1. Расширить `stats:analyze` на все типы ботов

Сейчас `php artisan stats:analyze` считает только **spot** (TradingBot + Trade). Имеет смысл:

- Добавить опциональный анализ по FuturesBot/FuturesTrade и BtcQuoteBot/BtcQuoteTrade.
- Либо отдельные команды `stats:analyze-futures` и `stats:analyze-btc-quote`, либо один `stats:analyze` с флагом `--type=all|spot|futures|btc-quote`.

**Зачем:** единая картина по эффективности всех ботов и корректная сохранённая статистика для дашборда (BotStatistics можно потом расширить на типы).

### 2. Алерты и лимиты для фьючерсов и BTC-quote

Для spot уже есть: max_daily_loss, max_drawdown, лимит открытых позиций, алерты в Telegram.

- Перенести аналогичную логику (или упрощённую) в `RunFuturesBotsCommand` и `RunBtcQuoteBotsCommand`: проверка дневного PnL, просадки, лимит открытых позиций по типу бота.
- При срабатывании — уведомление в Telegram (как для spot).

**Зачем:** контроль риска по фьючерсам и BTC-quote без отдельного дашборда.

### 3. Очередь для Telegram и тяжёлых задач

Сейчас уведомления и, при необходимости, синхронизация выполняются в том же процессе, что и `bots:run` / `futures:run` / `btc-quote:run`.

- Вынести отправку в Telegram в job (Laravel Queue) и ставить в очередь после принятия решения.
- При желании — вынести в очередь и `orders:sync` по одному боту.

**Зачем:** меньше задержек при сбоях Telegram/API, меньше риск таймаута по крону.

---

## 📊 Средний приоритет (неделя)

### 4. Мониторинг и здоровье системы

- Команда `health:check` или расширить существующий мониторинг: проверка OKX API (spot/futures), проверка Telegram, проверка что за последние N минут был хотя бы один успешный запуск `bots:run` / `futures:run` / `btc-quote:run`.
- При падении — алерт в Telegram (или в отдельный «технический» чат).

**Зачем:** быстрее замечать падения API, крона или сети.

### 5. Логирование решений ботов

- Писать в БД или в лог каждый сигнал (HOLD/BUY/SELL) с параметрами: бот_id, тип (spot/futures/btc-quote), символ, RSI, EMA, цена, причина (например «price_below_ema»).
- Опционально: простая страница в админке «Лог решений» с фильтром по боту и дате.

**Зачем:** разбор «почему не купил» и настройка порогов.

### 6. UX дашборда и ботов

- Фильтр по периоду на дашборде (последние 7/30/90 дней) для метрик и графиков.
- Экспорт сделок в CSV (по всем типам ботов).
- На страницах списка ботов: быстрый toggle «вкл/выкл» без перехода в редактирование.

**Зачем:** удобнее анализировать и управлять.

---

## 📈 Стратегия и аналитика (по желанию)

### 7. Дополнительные индикаторы

- MACD, Bollinger Bands, объём — как фильтры к текущей логике RSI+EMA (сначала в бэктесте, потом опция в настройках бота).
- ATR для динамического stop-loss (в процентах от ATR).

### 8. Бэктест для фьючерсов и BTC-quote

- Отдельные команды или режимы бэктеста по историческим данным для фьючерсов и BTC-quote с учётом комиссий и, по возможности, плеча.

**Зачем:** оценка стратегии до увеличения объёмов.

---

## 🧪 Качество кода и надёжность

### 9. Тесты

- Сейчас в проекте в основном тесты аутентификации.
- Имеет смысл добавить:
  - Unit: расчёт размера позиции, конвертация PnL BTC→USDT, логика «не покупать, если цена ниже EMA».
  - Feature: один прогон `bots:run` / `futures:run` / `btc-quote:run` в dry-run с моком API (без реальных ордеров).

**Зачем:** меньше регрессий при смене параметров и рефакторинге.

### 10. Обработка ошибок и повторные попытки

- В командах запуска ботов: явная обработка таймаутов и ошибок API (retry с экспоненциальной задержкой), логирование и при критичных сбоях — уведомление в Telegram.
- Не падать всем циклом из-за одного бота: обрабатывать по боту в try/catch, остальных продолжать.

**Зачем:** стабильная работа при нестабильной сети или бирже.

---

## Рекомендуемый порядок

1. **Сначала:** расширить `stats:analyze` на все типы ботов и при необходимости алерты/лимиты для фьючерсов и BTC-quote (п.1–2).
2. **Потом:** очередь для Telegram и health-check (п.3–4).
3. **Далее:** по необходимости — лог решений, UX, тесты и обработка ошибок (п.5–6, 9–10).
4. **По желанию:** индикаторы и бэктест (п.7–8).

Если скажете, с какого пункта начать (например, только `stats:analyze` или только алерты для фьючерсов), можно расписать конкретные шаги по коду.
