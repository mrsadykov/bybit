# ✅ Чек-лист перед переходом на реальную торговлю

## 🎯 Цель: Проверить покупку и продажу на сервере, затем активировать реальную торговлю

---

## 📋 ЧЕК-ЛИСТ ПРОВЕРКИ НА СЕРВЕРЕ:

### 1️⃣ Проверка текущего состояния бота:

```bash
# На сервере
cd /var/www/trading-bot
php artisan tinker
```

```php
// Проверить активного бота
$bot = \App\Models\TradingBot::where('is_active', true)->first();

if ($bot) {
    echo "Бот #{$bot->id}\n";
    echo "Символ: {$bot->symbol}\n";
    echo "Таймфрейм: {$bot->timeframe}\n";
    echo "Размер позиции: {$bot->position_size} USDT\n";
    echo "Stop-Loss: " . ($bot->stop_loss_percent ?? 'не установлен') . "%\n";
    echo "Take-Profit: " . ($bot->take_profit_percent ?? 'не установлен') . "%\n";
    echo "Dry Run: " . ($bot->dry_run ? 'ДА (тестовый режим)' : 'НЕТ (реальная торговля)') . "\n";
    echo "Активен: " . ($bot->is_active ? 'ДА' : 'НЕТ') . "\n";
} else {
    echo "Активных ботов не найдено\n";
}

exit
```

**✅ Проверить:**
- [ ] Бот активен (`is_active = true`)
- [ ] В тестовом режиме (`dry_run = true`)
- [ ] Параметры правильные (SL=3%, TP=6%)
- [ ] Размер позиции установлен (5 USDT)

---

### 2️⃣ Проверка баланса на бирже:

```bash
php artisan tinker
```

```php
// Проверить баланс USDT
$account = \App\Models\ExchangeAccount::where('exchange', 'okx')->first();
$okx = \App\Services\Exchanges\ExchangeServiceFactory::create($account);

$usdtBalance = $okx->getBalance('USDT');
$btcBalance = $okx->getBalance('BTC');

echo "USDT баланс: {$usdtBalance}\n";
echo "BTC баланс: {$btcBalance}\n";

// Проверить, что баланс достаточен
$bot = \App\Models\TradingBot::where('is_active', true)->first();
$minRequired = $bot->position_size + 5; // Позиция + запас

if ($usdtBalance >= $minRequired) {
    echo "✅ Баланс достаточен для торговли (нужно минимум {$minRequired} USDT)\n";
} else {
    echo "❌ Баланс недостаточен (нужно минимум {$minRequired} USDT)\n";
}

exit
```

**✅ Проверить:**
- [ ] Баланс USDT достаточен (минимум: размер позиции + 5 USDT)
- [ ] Баланс BTC доступен (если есть открытая позиция)

---

### 3️⃣ Проверка работы команды bots:run:

```bash
# Запустить бота вручную
php artisan bots:run
```

**✅ Проверить:**
- [ ] Команда выполняется без ошибок
- [ ] Цена получается корректно
- [ ] RSI и EMA рассчитываются
- [ ] Сигналы генерируются (HOLD/BUY/SELL)
- [ ] Telegram уведомления приходят

**Ожидаемый вывод:**
```
Запуск торговых ботов (Starting trading bots)...
------------------------------
Бот #1 (Bot #1)
Символ (Symbol): BTCUSDT
Текущая цена (Current price): 95XXX
RSI: XX.XX
EMA: XXXXX.XX
Сигнал (Signal): HOLD/BUY/SELL
Чистая позиция (Net position) (BTC): 0
...
```

---

### 4️⃣ Проверка работы команды orders:sync:

```bash
# Синхронизировать ордера
php artisan orders:sync
```

**✅ Проверить:**
- [ ] Команда выполняется без ошибок
- [ ] Ордера синхронизируются корректно
- [ ] Статусы обновляются (PENDING → SENT → FILLED)

---

### 5️⃣ Тестовая покупка (dry_run = true):

**Шаг 1: Убедиться, что бот в тестовом режиме:**

```bash
php artisan tinker
```

```php
$bot = \App\Models\TradingBot::where('is_active', true)->first();
$bot->dry_run = true; // Тестовый режим
$bot->is_active = true;
$bot->save();
echo "Бот в тестовом режиме (dry_run = true)\n";
exit
```

**Шаг 2: Запустить бота и дождаться BUY сигнала:**

```bash
php artisan bots:run
```

**Ожидаемое поведение:**
- Если сигнал BUY → должно показать "ТЕСТОВЫЙ РЕЖИМ BUY (DRY RUN BUY)"
- Telegram уведомление о тестовой покупке
- НО реальный ордер НЕ создается на бирже

**Шаг 3: Проверить, что ордер не создан на бирже:**

```bash
php artisan tinker
```

```php
// Проверить последние сделки
$trades = \App\Models\Trade::latest()->take(5)->get();
foreach ($trades as $trade) {
    echo "Trade #{$trade->id}: {$trade->side}, Status: {$trade->status}, Price: {$trade->price}\n";
}

exit
```

**✅ Проверить:**
- [ ] В тестовом режиме (`dry_run = true`) реальные ордера НЕ создаются
- [ ] Telegram уведомления приходят
- [ ] Сообщения в консоли правильные ("DRY RUN BUY")

---

### 6️⃣ Тестовая продажа (если есть открытая позиция):

**Если у вас есть открытая позиция BTC:**

```bash
php artisan bots:run
```

**Ожидаемое поведение при SELL сигнале:**
- Показать "ТЕСТОВЫЙ РЕЖИМ SELL (DRY RUN SELL)"
- Telegram уведомление о тестовой продаже
- НО реальный ордер НЕ создается на бирже

**✅ Проверить:**
- [ ] В тестовом режиме реальные ордера НЕ создаются
- [ ] Telegram уведомления приходят

---

### 7️⃣ Проверка Telegram уведомлений:

**✅ Проверить, что приходят уведомления:**

1. **При запуске bots:run:**
   - Уведомление "ЗАПУСК БОТОВ (BOTS RUN STARTED)"

2. **При HOLD сигнале:**
   - Уведомление "ДЕЙСТВИЙ НЕ ПРЕДПРИНЯТО (NO ACTION TAKEN)"

3. **При BUY сигнале (dry_run):**
   - Уведомление "ТЕСТОВАЯ ПОКУПКА (TEST BUY)"

4. **При SELL сигнале (dry_run):**
   - Уведомление "ТЕСТОВАЯ ПРОДАЖА (TEST SELL)"

---

### 8️⃣ Проверка логов:

```bash
# Мониторинг логов в реальном времени
tail -f storage/logs/laravel.log
```

**✅ Проверить:**
- [ ] Нет критических ошибок
- [ ] Нет ошибок API
- [ ] Все действия логируются корректно

---

## 🚨 ВАЖНЫЕ ПРОВЕРКИ ПЕРЕД РЕАЛЬНОЙ ТОРГОВЛЕЙ:

### ✅ 1. Все параметры правильные:

- [ ] Символ: BTCUSDT
- [ ] Таймфрейм: 1h
- [ ] Размер позиции: 5 USDT (минимальный для начала)
- [ ] Stop-Loss: 3%
- [ ] Take-Profit: 6%
- [ ] RSI период: 17
- [ ] EMA период: 10

### ✅ 2. Баланс достаточен:

- [ ] USDT баланс ≥ размер позиции + 5 USDT (запас)
- [ ] Рекомендуется: минимум 20-30 USDT для начала

### ✅ 3. Тестовый режим работает:

- [ ] Команды выполняются без ошибок
- [ ] Telegram уведомления приходят
- [ ] Логи корректные

### ✅ 4. Мониторинг настроен:

- [ ] Telegram уведомления работают
- [ ] Dashboard доступен
- [ ] Логи просматриваются

---

## 🚀 АКТИВАЦИЯ РЕАЛЬНОЙ ТОРГОВЛИ:

### ⚠️ ВНИМАНИЕ: Это необратимое действие!

После активации реальной торговли бот будет совершать реальные сделки с реальными деньгами!

---

### Шаг 1: Финальная проверка всех параметров:

```bash
php artisan tinker
```

```php
$bot = \App\Models\TradingBot::where('is_active', true)->first();

echo "=== ФИНАЛЬНАЯ ПРОВЕРКА ===\n";
echo "Бот #{$bot->id}\n";
echo "Символ: {$bot->symbol}\n";
echo "Размер позиции: {$bot->position_size} USDT\n";
echo "Stop-Loss: {$bot->stop_loss_percent}%\n";
echo "Take-Profit: {$bot->take_profit_percent}%\n";
echo "Текущий режим: " . ($bot->dry_run ? 'ТЕСТОВЫЙ (dry_run=true)' : 'РЕАЛЬНЫЙ (dry_run=false)') . "\n";
echo "Активен: " . ($bot->is_active ? 'ДА' : 'НЕТ') . "\n";

// Проверить баланс
$account = \App\Models\ExchangeAccount::where('exchange', 'okx')->first();
$okx = \App\Services\Exchanges\ExchangeServiceFactory::create($account);
$balance = $okx->getBalance('USDT');
echo "USDT баланс: {$balance} USDT\n";

exit
```

**✅ Убедиться:**
- Все параметры правильные
- Баланс достаточен
- Готовность к реальной торговле

---

### Шаг 2: Активировать реальную торговлю:

```bash
php artisan tinker
```

```php
// Найти активного бота
$bot = \App\Models\TradingBot::where('is_active', true)->first();

if (!$bot) {
    echo "Активных ботов не найдено\n";
    exit;
}

// ⚠️ ВАЖНО: Отключить dry_run (реальная торговля)
$bot->dry_run = false; // РЕАЛЬНАЯ ТОРГОВЛЯ
$bot->is_active = true;
$bot->save();

echo "✅ Реальная торговля активирована!\n";
echo "⚠️ Бот теперь будет совершать РЕАЛЬНЫЕ сделки!\n";
echo "Размер позиции: {$bot->position_size} USDT\n";
echo "Stop-Loss: {$bot->stop_loss_percent}%\n";
echo "Take-Profit: {$bot->take_profit_percent}%\n";

exit
```

---

### Шаг 3: Проверить, что реальная торговля активирована:

```bash
php artisan tinker
```

```php
$bot = \App\Models\TradingBot::where('is_active', true)->first();
echo "Dry Run: " . ($bot->dry_run ? 'ДА (тестовый)' : 'НЕТ (реальная торговля)') . "\n";
exit
```

**✅ Должно показать: "НЕТ (реальная торговля)"**

---

### Шаг 4: Первый запуск реальной торговли:

```bash
# Запустить бота в реальной торговле
php artisan bots:run
```

**Ожидаемое поведение:**
- Если сигнал BUY → создать реальный ордер на бирже
- Telegram уведомление о реальной покупке
- Ордер появится в истории ордеров на OKX

**⚠️ ВАЖНО:**
- Следите за выполнением команды
- Проверьте Telegram уведомления
- Проверьте логи

---

### Шаг 5: Проверить реальную покупку:

```bash
php artisan orders:sync
```

**Затем проверить в БД:**

```bash
php artisan tinker
```

```php
// Посмотреть последние сделки
$trades = \App\Models\Trade::latest()->take(5)->get();
foreach ($trades as $trade) {
    echo "Trade #{$trade->id}: {$trade->side}, Status: {$trade->status}, Quantity: {$trade->quantity}, Price: {$trade->price}\n";
}

exit
```

**✅ Проверить:**
- [ ] Сделка создана в БД (`side = BUY`)
- [ ] Статус обновлен (`status = FILLED` или `SENT`)
- [ ] Ордер появился на бирже OKX

---

## 📊 МОНИТОРИНГ ПОСЛЕ АКТИВАЦИИ:

### 1. Ежедневный мониторинг (первые дни):

**Через Telegram:**
- ✅ Уведомления о каждом BUY/SELL
- ✅ Уведомления об исполненных ордерах (FILLED)
- ✅ Ежедневная статистика (в 9:00)

**Через Dashboard:**
- ✅ Проверять статистику раз в день
- ✅ Отслеживать PnL
- ✅ Проверять открытые позиции

**Через команды:**
```bash
# Проверить статус бота
php artisan bots:run

# Синхронизировать ордера
php artisan orders:sync

# Проверить последние сделки
php artisan tinker
>>> \App\Models\Trade::latest()->take(5)->get();
```

### 2. При проблемах:

**Если что-то пошло не так:**
```bash
php artisan tinker
```

```php
// СРОЧНО вернуться в тестовый режим
$bot = \App\Models\TradingBot::where('is_active', true)->first();
$bot->dry_run = true; // Вернуть в тестовый режим
$bot->save();
echo "⚠️ Вернулись в тестовый режим!\n";
exit
```

---

## ⚠️ КРИТИЧЕСКИ ВАЖНО:

### 1. Начните с минимального размера позиции:

- ✅ **5 USDT** - минимальный размер для начала
- ✅ Не увеличивайте сразу - проверьте на практике
- ✅ Увеличивайте только после успешного тестирования (1-2 недели)

### 2. Мониторинг обязателен:

- ✅ Проверяйте ежедневно первые недели
- ✅ Отслеживайте все сделки через Telegram
- ✅ Контролируйте PnL через Dashboard

### 3. Готовность к убыткам:

- ✅ Торговля несет риски
- ✅ Возможны убыточные сделки
- ✅ Не инвестируйте больше, чем готовы потерять

### 4. При плохих результатах:

- ⚠️ Немедленно верните `dry_run = true`
- ⚠️ Проанализируйте проблему
- ⚠️ Попробуйте другие параметры

---

## 📋 ФИНАЛЬНЫЙ ЧЕК-ЛИСТ ПЕРЕД РЕАЛЬНОЙ ТОРГОВЛЕЙ:

### Обязательно проверить:

- [ ] ✅ Все параметры правильные (символ, таймфрейм, SL, TP)
- [ ] ✅ Размер позиции минимальный (5 USDT)
- [ ] ✅ Баланс достаточен (минимум 20-30 USDT)
- [ ] ✅ Тестовый режим работает корректно
- [ ] ✅ Telegram уведомления приходят
- [ ] ✅ Команды выполняются без ошибок
- [ ] ✅ Логи корректные (нет критических ошибок)
- [ ] ✅ Dashboard доступен
- [ ] ✅ Готовность к реальной торговле
- [ ] ✅ Понимание рисков

### После активации реальной торговли:

- [ ] ✅ Первый запуск прошел успешно
- [ ] ✅ Сделки синхронизируются корректно
- [ ] ✅ Мониторинг настроен (Telegram, Dashboard)
- [ ] ✅ План действий при проблемах готов

---

## 🎯 ИТОГОВАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ:

1. ✅ **Проверить текущее состояние бота** (шаг 1)
2. ✅ **Проверить баланс** (шаг 2)
3. ✅ **Запустить bots:run** (шаг 3)
4. ✅ **Запустить orders:sync** (шаг 4)
5. ✅ **Протестировать в dry_run режиме** (шаги 5-6)
6. ✅ **Проверить Telegram уведомления** (шаг 7)
7. ✅ **Проверить логи** (шаг 8)
8. ✅ **Финальная проверка всех параметров**
9. 🚀 **Активировать реальную торговлю** (`dry_run = false`)
10. ✅ **Первый запуск реальной торговли**
11. ✅ **Мониторинг результатов**

---

## 📚 Дополнительная информация:

- `NEXT_ACTIONS.md` - План действий
- `LOGS_LOCATION.md` - Где находятся логи
- `CHECK_BOT_COMMANDS.md` - Команды для проверки ботов

---

## ⚠️ ДИСКЛЕЙМЕР:

Реальная торговля несет финансовые риски. Всегда проверяйте работоспособность бота перед активацией реальной торговли. Начинайте с минимальных сумм и увеличивайте только после успешного тестирования.
