# 🔐 Проверка прав API ключа Bybit

## ❌ Ошибка 401 (Unauthorized)

Если публичный API работает, но приватный возвращает 401, проблема в:
1. **Правах API ключа** (самая частая причина)
2. **IP Whitelist** (блокирует запросы)
3. **Неправильный API Secret**

## ✅ Что проверить на bybit.com

### 1. Права API ключа

1. Зайдите на bybit.com (или testnet.bybit.com)
2. **API Management** → выберите ваш ключ
3. Проверьте что включены:
   - ✅ **Read** (обязательно для получения баланса)
   - ✅ **Trade** (если планируете торговать)

### 2. IP Whitelist

1. В настройках API ключа
2. Проверьте **IP Whitelist**
3. Если включен:
   - Добавьте ваш IP адрес
   - Или **отключите для тестирования**

### 3. Статус ключа

- Убедитесь что ключ **активен** (не отозван)
- Проверьте что ключ не истек

## 🛠️ Решение

### Вариант 1: Обновить права существующего ключа

1. На bybit.com включите **Read** permission
2. Отключите IP whitelist (для теста)
3. Попробуйте снова: `php artisan api:test --account=8`

### Вариант 2: Создать новый ключ

1. Создайте новый API ключ на bybit.com
2. Убедитесь что включены: **Read** + **Trade**
3. IP whitelist: **отключен** (для теста)
4. Обновите `.env`:
   ```env
   BYBIT_API_KEY=новый_ключ
   BYBIT_API_SECRET=новый_секрет
   ```
5. Пересоздайте аккаунт:
   ```bash
   php artisan create-bybit-account
   ```
6. Проверьте:
   ```bash
   php artisan api:test --account=8
   ```

## 🔍 Диагностика

Используйте команду диагностики:

```bash
php artisan api:test --account=8
```

Она покажет:
- ✅ Правильность ключей
- ✅ Расшифровку секрета
- ✅ Работу публичного API
- ✅ Проблему с приватным API

## ⚠️ Важно

- **API Secret показывается только один раз!** Сохраните его сразу
- Для testnet используйте ключи от **testnet.bybit.com**
- Для production используйте ключи от **bybit.com**
- Не путайте ключи между testnet и production

## 📝 Чеклист

- [ ] API ключ имеет **Read** permission
- [ ] IP whitelist отключен (или ваш IP добавлен)
- [ ] API ключ активен (не отозван)
- [ ] API Secret правильный (скопирован полностью)
- [ ] Ключи от правильного окружения (testnet/production)
