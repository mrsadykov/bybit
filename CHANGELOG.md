# Changelog - Улучшения торгового бота

## [2025-01-12] - Критичные улучшения

### ✅ Добавлено

#### 1. Проверка баланса перед покупкой
- Добавлен метод `getBalance()` в `BybitService` для получения баланса USDT
- Добавлен метод `getAllBalances()` для получения всех балансов
- Проверка баланса перед выполнением BUY ордера
- Логирование предупреждений при недостаточном балансе

#### 2. Валидация минимальных сумм
- Проверка минимальной суммы ордера (`min_notional_usdt`) перед покупкой
- Валидация при создании бота через `CreateBybitTradingBotCommand`

#### 3. Улучшенное логирование
- Структурированное логирование всех торговых операций:
  - Инициация BUY ордера
  - Исполнение ордера (FILLED)
  - Отправка ордера (SENT)
  - Закрытие позиции с расчетом PnL
  - Ошибки проверки баланса

#### 4. Обработка частично исполненных ордеров
- Добавлена поддержка статуса `PARTIALLY_FILLED`
- Обновление количества при частичном исполнении
- Синхронизация частично исполненных ордеров в `SyncOrdersCommand`
- Закрытие позиции только при полном исполнении SELL

#### 5. Валидация входных данных
- Валидация формата символа (BTCUSDT, ETHUSDT и т.д.)
- Валидация таймфрейма (с предупреждением)
- Валидация стратегии (с предупреждением)
- Валидация минимальной суммы при создании бота

### 🔧 Изменено

- `RunTradingBotsCommand`: добавлена проверка баланса и минимальной суммы перед BUY
- `SyncOrdersCommand`: добавлена обработка `PARTIALLY_FILLED` статуса
- `BybitService`: добавлены методы для работы с балансом
- `Trade` модель: обновлен комментарий для статуса (добавлен PARTIALLY_FILLED)

### 📝 Технические детали

#### Новые методы в BybitService:
```php
getBalance(string $coin = 'USDT'): float
getAllBalances(): array
```

#### Новые проверки в RunTradingBotsCommand:
- Проверка минимальной суммы ордера
- Проверка баланса USDT перед покупкой
- Улучшенное логирование операций

#### Новые статусы Trade:
- `PARTIALLY_FILLED` - ордер исполнен частично

---

## Следующие шаги

### Планируется:
1. Rate limiting для API запросов
2. Retry механизм для временных ошибок
3. Кэширование цен
4. Unit-тесты для индикаторов
5. Мониторинг и метрики
