# 🚀 Пошаговая инструкция: Создание и запуск ETHUSDT бота

## 📋 ЧТО БУДЕТ ПРОИСХОДИТЬ

### Этап 1: Создание бота в базе данных

**Команда:**
```bash
php artisan okx-bot:create ETHUSDT 1h rsi_ema 5 --stop-loss=3.0 --take-profit=6.0
```

**Что происходит:**
1. ✅ Проверка наличия OKX аккаунта в базе данных
2. ✅ Валидация параметров (символ, таймфрейм, размер позиции)
3. ✅ Создание записи в таблице `trading_bots`:
   - `symbol`: ETHUSDT
   - `timeframe`: 1h
   - `strategy`: rsi_ema
   - `position_size`: 5.00000000 USDT
   - `stop_loss_percent`: 3.00%
   - `take_profit_percent`: 6.00%
   - `is_active`: false (неактивен по умолчанию)
   - `exchange_account_id`: ID вашего OKX аккаунта
4. ✅ Вывод информации о созданном боте

**Результат:** Бот создан, но **НЕ активен** (не будет торговать)

---

### Этап 2: Активация бота

**Команда:**
```bash
php artisan tinker
```

**В tinker:**
```php
$bot = \App\Models\TradingBot::find(2); // замените 2 на ID нового бота
$bot->is_active = true;
$bot->save();
exit
```

**Что происходит:**
1. ✅ Открывается интерактивная консоль Laravel
2. ✅ Находится бот по ID (будет показан при создании)
3. ✅ Устанавливается `is_active = true`
4. ✅ Сохранение в базу данных

**Результат:** Бот **активирован** и готов к торговле

---

### Этап 3: Проверка ботов

**Команда:**
```bash
php artisan tinker
```

**В tinker:**
```php
\App\Models\TradingBot::with('exchangeAccount')->get()->each(function($bot) {
    echo "Bot #{$bot->id}: {$bot->symbol}, ";
    echo "active: " . ($bot->is_active ? 'yes' : 'no') . ", ";
    echo "exchange: " . ($bot->exchangeAccount->exchange ?? 'none') . "\n";
});
exit
```

**Что происходит:**
1. ✅ Загружаются все боты из базы данных
2. ✅ Выводится информация:
   - ID бота
   - Торговая пара (BTCUSDT, ETHUSDT)
   - Статус активности (yes/no)
   - Биржа (okx, bybit)

**Результат:** Видите список всех ботов и их статусы

---

### Этап 4: Запуск ботов (первый раз)

**Команда:**
```bash
php artisan bots:run
```

**Что происходит (для каждого активного бота):**

#### 4.1. Получение данных
1. ✅ Загружается информация о боте из БД
2. ✅ Получается текущая цена ETHUSDT через OKX API
3. ✅ Получаются свечи (candles) за последний период через OKX API
   - Таймфрейм: 1h
   - Количество: ~300 свечей (лимит OKX)

#### 4.2. Расчет индикаторов
1. ✅ **RSI расчет:**
   - Период: 17
   - Результат: значение от 0 до 100
   - Пример: RSI = 45.23

2. ✅ **EMA расчет:**
   - Период: 10
   - Результат: средняя цена
   - Пример: EMA = 2850.45

#### 4.3. Принятие решения (стратегия)
1. ✅ **Анализ сигнала:**
   - Текущая цена ETH: например, $2900
   - RSI: например, 25 (низкий)
   - EMA: например, $2850
   - Проверка условий:
     - BUY: RSI < 30 И цена > EMA → если RSI=25 и цена=2900 > EMA=2850 → **BUY**
     - SELL: RSI > 70 И цена < EMA → если RSI=75 и цена=2900 < EMA=2950 → **SELL**
     - HOLD: иначе → **HOLD**

#### 4.4. Проверка позиции
1. ✅ **PositionManager проверяет:**
   - Есть ли открытые BUY позиции?
   - Есть ли pending SELL ордера?
   - Чистая позиция (netPosition):
     - Если > 0 → есть открытая позиция
     - Если = 0 → нет позиции

#### 4.5. Действия в зависимости от сигнала

**Если сигнал = BUY:**
- ✅ Проверка: `canBuy()` → true только если netPosition <= 0
- ✅ Если можно покупать:
  1. Расчет суммы: 5 USDT
  2. Получение текущей цены ETH
  3. Создание записи в таблице `trades`:
     - `side`: BUY
     - `status`: PENDING
     - `price`: текущая цена
     - `quantity`: 0 (узнаем после исполнения)
  4. Отправка ордера на OKX:
     - Тип: Market Buy
     - Сумма: 5 USDT
  5. Ожидание 0.5 секунды
  6. Проверка статуса ордера
  7. Если FILLED:
     - Обновление `trades`: `status = FILLED`, `quantity = купленное количество`
     - Отправка уведомления в Telegram: ✅ ПОКУПКА ИСПОЛНЕНА
- ❌ Если нельзя покупать (уже есть позиция):
  - Пропуск BUY
  - Уведомление: ⚠️ BUY пропущен: позиция уже открыта

**Если сигнал = SELL:**
- ✅ Проверка: `canSell()` → true только если netPosition > 0
- ✅ Если можно продавать:
  1. Получение доступного баланса ETH
  2. Создание записи в таблице `trades`:
     - `side`: SELL
     - `status`: PENDING
     - `quantity`: весь доступный баланс ETH
  3. Отправка ордера на OKX:
     - Тип: Market Sell
     - Количество: весь доступный ETH
  4. Ожидание 0.5 секунды
  5. Проверка статуса ордера
  6. Если FILLED:
     - Обновление `trades`: `status = FILLED`
     - Закрытие соответствующих BUY позиций (FIFO)
     - Расчет PnL (прибыль/убыток)
     - Отправка уведомления в Telegram: 💰 ПРОДАЖА ИСПОЛНЕНА
- ❌ Если нельзя продавать (нет позиции):
  - Пропуск SELL
  - Уведомление: ⚠️ Нет открытой BUY позиции — пропуск SELL

**Если сигнал = HOLD:**
- ✅ Отправка уведомления в Telegram:
  - ⏸️ ДЕЙСТВИЙ НЕ ПРЕДПРИНЯТО
  - Символ: ETHUSDT
  - Цена: $2900
  - Сигнал: HOLD
  - RSI: 45.23
  - EMA: 2850.45
  - Время: 2026-01-19 10:00:00

#### 4.6. Завершение
- ✅ Обновление `last_trade_at` в таблице `trading_bots`
- ✅ Переход к следующему боту (если есть)

**Результат:** Боты обработаны, сделки выполнены (если были сигналы)

---

### Этап 5: Автоматический запуск (через cron)

**Настройка cron (уже должно быть настроено):**
```bash
* * * * * cd /var/www/trading-bot && php artisan schedule:run >> /dev/null 2>&1
```

**Что происходит каждую минуту:**
1. ✅ Laravel Scheduler запускается
2. ✅ Проверяет расписание команд
3. ✅ Если пришло время → запускает `bots:run`

**В `routes/console.php` должно быть:**
```php
Schedule::command('bots:run')->everyFiveMinutes();
```

**Результат:** Боты запускаются автоматически каждые 5 минут

---

### Этап 6: Синхронизация ордеров (важно!)

**Команда:**
```bash
php artisan orders:sync
```

**Что происходит:**
1. ✅ Находятся все ордера со статусом PENDING или SENT
2. ✅ Для каждого ордера:
   - Запрос статуса на OKX API
   - Если FILLED:
     - Обновление `trades`: `status = FILLED`, `quantity = исполненное количество`
     - Закрытие позиций
     - Расчет PnL
   - Если FAILED:
     - Обновление `trades`: `status = FAILED`
3. ✅ Проверка Stop-Loss и Take-Profit:
   - Если цена упала на 3% от цены покупки → автоматический SELL (Stop-Loss)
   - Если цена выросла на 6% от цены покупки → автоматический SELL (Take-Profit)

**Результат:** Все ордера синхронизированы, SL/TP проверены

**Рекомендация:** Запускать каждую минуту через cron:
```bash
* * * * * cd /var/www/trading-bot && php artisan orders:sync >> /dev/null 2>&1
```

---

## 📊 ПОЛНЫЙ ЦИКЛ РАБОТЫ БОТА

### Пример сценария:

**День 1, 10:00:**
- Сигнал: HOLD
- Действие: Уведомление в Telegram
- Позиция: Нет

**День 1, 14:00:**
- Сигнал: BUY (RSI=25, цена > EMA)
- Действие: Покупка 5 USDT ETH
- Результат: Куплено 0.0017 ETH по цене $2900
- Позиция: Открыта (0.0017 ETH)

**День 1, 18:00:**
- Сигнал: HOLD
- Действие: Уведомление
- Позиция: Открыта (0.0017 ETH)
- Текущая цена: $2920 (+0.69%)
- Stop-Loss: $2813 (-3%)
- Take-Profit: $3074 (+6%)

**День 2, 08:00:**
- Сигнал: SELL (RSI=75, цена < EMA)
- Действие: Продажа всего ETH (0.0017)
- Результат: Продано по цене $3050
- PnL: +$0.255 (+5.17%)
- Позиция: Закрыта

**День 2, 12:00:**
- Сигнал: HOLD
- Действие: Уведомление
- Позиция: Нет

---

## 🔔 УВЕДОМЛЕНИЯ В TELEGRAM

### Типы уведомлений:

1. **⏸️ ДЕЙСТВИЙ НЕ ПРЕДПРИНЯТО (HOLD)**
   - При каждом запуске, если сигнал HOLD
   - Содержит: символ, цену, RSI, EMA, время

2. **✅ ПОКУПКА ИСПОЛНЕНА (BUY FILLED)**
   - Когда ордер BUY исполнен
   - Содержит: символ, количество, цену, комиссию, время

3. **💰 ПРОДАЖА ИСПОЛНЕНА (SELL FILLED)**
   - Когда ордер SELL исполнен
   - Содержит: символ, количество, цену, комиссию, PnL, время

4. **⚠️ СДЕЛКА ПРОПУЩЕНА (SKIP)**
   - Когда сигнал есть, но позиция не позволяет
   - Содержит: причину пропуска

5. **❌ ОШИБКА (ERROR)**
   - При ошибках API или торговли
   - Содержит: описание ошибки

---

## 📈 МОНИТОРИНГ И ПРОВЕРКА

### Проверка сделок:
```bash
php artisan tinker
>>> \App\Models\Trade::with('tradingBot')->latest()->take(10)->get()->each(function($t) {
    echo "Trade #{$t->id}: {$t->side} {$t->symbol} @ \${$t->price}, qty: {$t->quantity}, status: {$t->status}\n";
});
>>> exit
```

### Проверка баланса:
```bash
php artisan balance:check --exchange=okx
```

### Проверка логов:
```bash
tail -f /var/www/trading-bot/storage/logs/laravel.log
```

---

## ✅ ЧЕКЛИСТ ПЕРЕД ЗАПУСКОМ

- [ ] OKX аккаунт создан и настроен
- [ ] API ключи OKX добавлены в `.env`
- [ ] Баланс на OKX достаточен (минимум 10 USDT для двух ботов)
- [ ] Telegram бот настроен и работает
- [ ] Cron настроен для автоматического запуска
- [ ] Бот создан через `okx-bot:create`
- [ ] Бот активирован (`is_active = true`)
- [ ] Проверен список ботов
- [ ] Первый запуск выполнен вручную (`bots:run`)
- [ ] Уведомления приходят в Telegram
- [ ] Синхронизация ордеров работает (`orders:sync`)

---

## 🎯 ИТОГ

После выполнения всех шагов:
- ✅ ETHUSDT бот создан и активирован
- ✅ Работает параллельно с BTCUSDT ботом
- ✅ Автоматически запускается каждые 5 минут
- ✅ Отправляет уведомления в Telegram
- ✅ Выполняет сделки при сигналах BUY/SELL
- ✅ Защищен Stop-Loss (3%) и Take-Profit (6%)
