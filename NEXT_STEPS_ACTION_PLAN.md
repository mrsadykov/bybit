# 🎯 План действий: Что делать дальше

## 📊 Текущая ситуация

### Таблица trading_bots:
- ✅ Все боты активны (`is_active = 1`)
- ⚠️ `rsi_period` и `ema_period` = NULL у всех ботов
- ✅ Используются дефолтные значения (RSI=17, EMA=10)

---

## ✅ ШАГ 1: Установить периоды RSI/EMA

### Рекомендуемые значения:

| Бот | Символ | RSI | EMA | Примечание |
|-----|--------|-----|-----|------------|
| #1 | BTCUSDT | 17 | 10 | ✅ Дефолтные подходят |
| #2 | ETHUSDT | 17 | 10 | ✅ Дефолтные подходят |
| #3 | SOLUSDT | 17 | **15** | ⚠️ **ВАЖНО: EMA=15, не 10!** |

### Команда для установки:

```bash
php artisan tinker
```

```php
// Bot #1 (BTCUSDT)
$bot = \App\Models\TradingBot::find(1);
$bot->rsi_period = 17;
$bot->ema_period = 10;
$bot->save();

// Bot #2 (ETHUSDT)
$bot = \App\Models\TradingBot::find(2);
$bot->rsi_period = 17;
$bot->ema_period = 10;
$bot->save();

// Bot #3 (SOLUSDT) - ВАЖНО: EMA=15!
$bot = \App\Models\TradingBot::find(3);
$bot->rsi_period = 17;
$bot->ema_period = 15;
$bot->save();

// Проверить
\App\Models\TradingBot::all()->each(function($bot) {
    echo "Bot #{$bot->id} ({$bot->symbol}): RSI={$bot->rsi_period}, EMA={$bot->ema_period}\n";
});

exit
```

**Или через SQL:**
```sql
UPDATE trading_bots SET rsi_period = 17, ema_period = 10 WHERE id IN (1, 2);
UPDATE trading_bots SET rsi_period = 17, ema_period = 15 WHERE id = 3;
```

---

## ✅ ШАГ 2: Проверить ежедневную статистику в Telegram

### Расписание:
- **Время:** Каждый день в 09:00
- **Команда:** `telegram:daily-stats`
- **Настроено в:** `routes/console.php`

### Проверка работы:

#### 1. Запустить вручную (для теста):

```bash
php artisan telegram:daily-stats
```

**Ожидаемый результат:**
- Команда выполнится без ошибок
- В Telegram придет сообщение со статистикой
- В консоли: "✅ Статистика отправлена пользователю..."

#### 2. Проверить расписание:

```bash
# Проверить routes/console.php
cat routes/console.php | grep daily-stats

# Должно быть:
# Schedule::command('telegram:daily-stats')->dailyAt('09:00');
```

#### 3. Проверить cron:

```bash
crontab -l
```

Должна быть строка:
```
* * * * * cd /var/www/trading-bot && php artisan schedule:run >> /dev/null 2>&1
```

#### 4. Проверить логи:

```bash
# Посмотреть последние логи
tail -n 100 storage/logs/laravel.log | grep -i "daily\|telegram"

# Или следить в реальном времени
tail -f storage/logs/laravel.log
```

---

## ✅ ШАГ 3: Проверить работу ботов

### Запустить ботов:

```bash
php artisan bots:run
```

**Проверить:**
- ✅ Боты запускаются без ошибок
- ✅ Показывают правильные периоды: "RSI (17): ...", "EMA (15): ..." для SOL
- ✅ Сигналы генерируются корректно
- ✅ Telegram уведомления приходят

### Проверить логи:

```bash
tail -f storage/logs/laravel.log
```

---

## ✅ ШАГ 4: Мониторинг и оптимизация

### Ежедневные задачи:

1. **Проверять Telegram уведомления:**
   - Ежедневная статистика в 09:00
   - Уведомления о сделках
   - Уведомления об ошибках

2. **Мониторить работу ботов:**
   ```bash
   php artisan bots:run
   ```

3. **Проверять баланс:**
   ```bash
   php artisan balance:check --exchange=okx
   ```

4. **Проверять сделки:**
   ```bash
   php artisan tinker
   ```
   ```php
   \App\Models\Trade::latest()->take(10)->get();
   ```

### Еженедельные задачи:

1. **Анализ результатов:**
   - Проверить PnL
   - Проанализировать win rate
   - Оценить эффективность стратегии

2. **Оптимизация (опционально):**
   ```bash
   php artisan strategy:optimize
   ```

---

## 📋 Чеклист "Что делать дальше"

### Немедленно (сегодня):

- [ ] **Установить периоды RSI/EMA для ботов** (особенно SOLUSDT: EMA=15)
- [ ] **Проверить ежедневную статистику:** `php artisan telegram:daily-stats`
- [ ] **Проверить работу ботов:** `php artisan bots:run`
- [ ] **Убедиться, что cron работает:** `crontab -l`

### В ближайшее время (на этой неделе):

- [ ] Мониторить работу ботов ежедневно
- [ ] Проверять Telegram уведомления
- [ ] Анализировать результаты сделок
- [ ] При необходимости скорректировать параметры

### В будущем (когда накопится статистика):

- [ ] Проанализировать эффективность стратегии
- [ ] Рассмотреть оптимизацию параметров
- [ ] Возможно добавить больше ботов для других пар
- [ ] Расширить стратегию или добавить новые индикаторы

---

## 🎯 Приоритетные действия

### 1. Установить EMA=15 для SOLUSDT (ВАЖНО!)

```bash
php artisan tinker
```

```php
$bot = \App\Models\TradingBot::find(3);
$bot->ema_period = 15;
$bot->save();
echo "✅ SOLUSDT bot updated: EMA=15\n";
exit
```

### 2. Проверить ежедневную статистику

```bash
# Запустить вручную для теста
php artisan telegram:daily-stats

# Проверить, что пришло в Telegram
```

### 3. Убедиться, что все работает

```bash
# Запустить ботов
php artisan bots:run

# Проверить логи
tail -f storage/logs/laravel.log
```

---

## ✅ Готово!

После выполнения этих шагов:
- ✅ Боты будут использовать оптимальные периоды RSI/EMA
- ✅ Ежедневная статистика будет приходить в Telegram
- ✅ Все будет работать стабильно

**Удачи в торговле!** 🚀
