# Быстрый старт

## ✅ Что уже исправлено:

1. **Проблема с версией PHP** - отключена проверка platform-check
2. **Artisan команды работают** - можно использовать все команды

## 🚀 Запуск проекта

### 1. Откат и повторное применение миграций

**Вариант 1: С сохранением данных (откатить по одной)**
```bash
php artisan migrate:rollback
php artisan migrate
```

**Вариант 2: Полный сброс (удалит все данные!)**
```bash
php artisan migrate:fresh
```

**Вариант 3: Использовать скрипт**
```bash
./reset-migrations.sh
```

### 2. Запуск сервера

```bash
php artisan serve
```

Или используйте скрипт из composer:
```bash
composer run dev
```

## 🔍 Навигация по классам в PhpStorm

### Автоматически работает:
- **Cmd+Click** (Mac) / **Ctrl+Click** (Windows/Linux) - переход к определению
- **Cmd+B** - переход к определению
- **Cmd+Alt+B** - найти все реализации
- **Cmd+N** - найти класс
- **Cmd+Shift+N** - найти файл

### Если не работает:

1. **Переиндексировать проект**:
   - File → Invalidate Caches → Invalidate and Restart

2. **Обновить автозагрузку** (выполните в терминале):
   ```bash
   composer dump-autoload
   ```

3. **Проверить настройки**:
   - Settings → PHP → Composer
   - Убедитесь, что путь к `composer` указан правильно

4. **Проверить плагины**:
   - Settings → Plugins
   - Убедитесь, что установлены: PHP, Composer

### Полезные горячие клавиши PhpStorm:
- **Cmd+O** - найти класс
- **Cmd+Shift+O** - найти файл  
- **Cmd+Alt+O** - найти символ
- **Cmd+Q** - быстрая документация
- **Alt+F7** - найти использование
- **Cmd+E** - недавние файлы

## 📋 Проверка команд

После настройки проверьте команды:

```bash
# Список всех команд
php artisan list

# Настройка проекта
php artisan setup

# Создание аккаунта Bybit
php artisan create-bybit-account

# Создание торгового бота
php artisan bybit-bot:create BTCUSDT 5m rsi_ema 10

# Запуск ботов
php artisan bots:run

# Синхронизация ордеров
php artisan orders:sync
```

## ⚠️ Важные замечания

1. **Версия PHP**: У вас PHP 8.2.27, а Laravel 12 требует 8.4+. 
   - Я временно отключил проверку для разработки
   - Для продакшена рекомендуется обновить PHP до 8.4+

2. **Миграции**: Все миграции уже применены (batch 2-8)
   - Если нужно пересоздать БД, используйте `migrate:fresh`
   - Это удалит все данные!

3. **Автозагрузка**: Если навигация не работает, выполните:
   ```bash
   composer dump-autoload
   ```
   И переиндексируйте проект в PhpStorm.
