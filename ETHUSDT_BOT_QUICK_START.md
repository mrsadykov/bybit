# 🚀 Быстрый старт: Создание ETHUSDT бота

## ✅ Параметры бота:
- **Symbol:** ETHUSDT
- **Timeframe:** 1h
- **Strategy:** rsi_ema (RSI период 17, EMA период 10)
- **Position Size:** 5 USDT
- **Stop-Loss:** 3.0%
- **Take-Profit:** 6.0%

---

## 📋 ШАГИ:

### Шаг 1: Создание бота

```bash
php artisan okx-bot:create ETHUSDT 1h rsi_ema 5 --stop-loss=3.0 --take-profit=6.0
```

**Ожидаемый результат:**
```
✅ OKX TradingBot created: #2
   Symbol: ETHUSDT
   Timeframe: 1h
   Strategy: rsi_ema
   Position size: 5.00000000 USDT
   Stop-Loss: -3.00%
   Take-Profit: +6.00%
   Status: Inactive (use tinker to activate)
```

**⚠️ Запомните ID бота (например, #2)** - он понадобится в следующем шаге!

---

### Шаг 2: Активация бота

```bash
php artisan tinker
```

**В tinker (замените 2 на ваш ID бота):**
```php
$bot = \App\Models\TradingBot::find(2);
$bot->is_active = true;
$bot->save();
exit
```

**Ожидаемый результат:** Бот активирован, но пока ничего не происходит

---

### Шаг 3: Проверка всех ботов

```bash
php artisan tinker
```

**В tinker:**
```php
\App\Models\TradingBot::with('exchangeAccount')->get()->each(function($bot) {
    echo "Bot #{$bot->id}: {$bot->symbol}, ";
    echo "active: " . ($bot->is_active ? 'yes' : 'no') . ", ";
    echo "exchange: " . ($bot->exchangeAccount->exchange ?? 'none') . "\n";
});
exit
```

**Ожидаемый результат:**
```
Bot #1: BTCUSDT, active: yes, exchange: okx
Bot #2: ETHUSDT, active: yes, exchange: okx
```

---

### Шаг 4: Первый запуск (проверка)

```bash
php artisan bots:run
```

**Что произойдет:**
- ✅ Оба бота (BTC и ETH) запустятся
- ✅ Получат свечи с биржи
- ✅ Рассчитают RSI и EMA
- ✅ Примут решение (BUY/SELL/HOLD)
- ✅ Отправят уведомления в Telegram

**Ожидаемый результат:**
- В консоли увидите информацию о каждом боте
- В Telegram получите уведомления:
  - ⏸️ ДЕЙСТВИЙ НЕ ПРЕДПРИНЯТО для BTCUSDT
  - ⏸️ ДЕЙСТВИЙ НЕ ПРЕДПРИНЯТО для ETHUSDT

---

### Шаг 5: Проверка автоматического запуска

Боты должны запускаться автоматически каждые 5 минут через cron.

**Проверка cron (на сервере):**
```bash
crontab -l
```

**Должно быть:**
```
* * * * * cd /var/www/trading-bot && php artisan schedule:run >> /dev/null 2>&1
```

**Проверка расписания в `routes/console.php`:**
```php
Schedule::command('bots:run')->everyFiveMinutes();
```

---

### Шаг 6: Мониторинг (опционально)

**Проверка сделок:**
```bash
php artisan tinker
>>> \App\Models\Trade::with('tradingBot')->latest()->take(5)->get()->each(function($t) {
    echo "Trade #{$t->id}: {$t->side} {$t->symbol} @ \${$t->price}, qty: {$t->quantity}, status: {$t->status}\n";
});
>>> exit
```

**Проверка баланса:**
```bash
php artisan balance:check --exchange=okx
```

**Просмотр логов:**
```bash
tail -f /var/www/trading-bot/storage/logs/laravel.log
```

---

## ✅ ЧТО ДАЛЬШЕ?

После выполнения всех шагов:

1. ✅ **ETHUSDT бот создан и активирован**
2. ✅ **Работает параллельно с BTCUSDT ботом**
3. ✅ **Автоматически запускается каждые 5 минут**
4. ✅ **Отправляет уведомления в Telegram**
5. ✅ **Выполняет сделки при сигналах BUY/SELL**
6. ✅ **Защищен Stop-Loss (3%) и Take-Profit (6%)**

---

## 🔔 УВЕДОМЛЕНИЯ В TELEGRAM

Вы будете получать уведомления:

- **⏸️ ДЕЙСТВИЙ НЕ ПРЕДПРИНЯТО** - когда сигнал HOLD (каждые 5 минут)
- **✅ ПОКУПКА ИСПОЛНЕНА** - когда бот купил ETH
- **💰 ПРОДАЖА ИСПОЛНЕНА** - когда бот продал ETH
- **⚠️ СДЕЛКА ПРОПУЩЕНА** - когда сигнал есть, но позиция не позволяет

---

## ⚠️ ВАЖНО

1. **Баланс:** Убедитесь, что на OKX достаточно USDT для обоих ботов (минимум 10 USDT)
2. **Синхронизация:** Команда `orders:sync` должна запускаться каждую минуту (для проверки SL/TP)
3. **Мониторинг:** Следите за уведомлениями в Telegram

---

## 🎯 ГОТОВО!

Теперь у вас работают два бота:
- **BTCUSDT** на таймфрейме 1h
- **ETHUSDT** на таймфрейме 1h

Оба используют стратегию RSI+EMA с одинаковыми параметрами защиты (SL 3%, TP 6%).
