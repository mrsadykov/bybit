# 🧪 Тестирование на Bybit Testnet

## 📋 Пошаговая инструкция

### Шаг 1: Регистрация на Bybit Testnet

1. Перейдите на: https://testnet.bybit.com/
2. Зарегистрируйтесь или войдите
3. Получите тестовые USDT (обычно дают бесплатно для тестирования)

### Шаг 2: Создание API ключей для Testnet

1. Войдите в аккаунт testnet
2. Перейдите в **API Management** (или **API Keys**)
3. Создайте новый API ключ:
   - **API Key Name**: `TradingBot` (любое имя)
   - **Permissions**: 
     - ✅ **Read** (обязательно)
     - ✅ **Trade** (обязательно для торговли)
   - **IP Whitelist**: можно оставить пустым для тестирования
4. Сохраните:
   - **API Key**
   - **Secret Key** (показывается только один раз!)

### Шаг 3: Настройка .env файла

Откройте `.env` и настройте:

```env
# Режим торговли - FALSE для тестирования
REAL_TRADING=false

# Bybit окружение - testnet
BYBIT_ENV=testnet

# API ключи от testnet
BYBIT_API_KEY=your_testnet_api_key_here
BYBIT_API_SECRET=your_testnet_secret_key_here

# Админ пользователь
APP_ADMIN_EMAIL=your@email.com
APP_ADMIN_PASSWORD=your_password
APP_ADMIN_NAME=Admin
```

### Шаг 4: Очистка кэша конфигурации

```bash
php artisan config:clear
php artisan cache:clear
```

### Шаг 5: Настройка проекта

```bash
# Создать админа и аккаунт Bybit
php artisan setup
```

Эта команда:
- Создаст админ пользователя
- Создаст ExchangeAccount с вашими API ключами
- Установит `is_testnet = true` автоматически

### Шаг 6: Проверка подключения к API

```bash
php artisan tinker
```

```php
// Проверить аккаунт
>>> $account = \App\Models\ExchangeAccount::first();
>>> $account->is_testnet; // должно быть true

// Проверить баланс
>>> $bybit = new \App\Services\Exchanges\Bybit\BybitService($account);
>>> $bybit->getBalance('USDT'); // должен показать баланс testnet

// Проверить цену
>>> $bybit->getPrice('BTCUSDT'); // должна вернуть цену

// Выход
>>> exit
```

Если все работает - вы увидите баланс и цену. Если ошибка - проверьте API ключи.

### Шаг 7: Создание тестового бота

```bash
# Создать бота с небольшой суммой для теста
php artisan bybit-bot:create BTCUSDT 5m rsi_ema 5

# Параметры:
# - BTCUSDT - торговая пара
# - 5m - таймфрейм (5 минут)
# - rsi_ema - стратегия
# - 5 - размер позиции в USDT (минимум 1, но для теста можно 5)
```

### Шаг 8: Активация бота

```bash
php artisan tinker
```

```php
// Найти созданного бота
>>> $bot = \App\Models\TradingBot::latest()->first();
>>> $bot->id; // запомните ID

// Активировать бота
>>> $bot->update(['is_active' => true]);

// Проверить
>>> $bot->refresh();
>>> $bot->is_active; // должно быть true

// Проверить настройки
>>> $bot->symbol;
>>> $bot->timeframe;
>>> $bot->position_size;
>>> $bot->dry_run; // должно быть true (по умолчанию)

>>> exit
```

### Шаг 9: Тестовый запуск (DRY RUN)

**Важно**: По умолчанию бот работает в DRY RUN режиме (не совершает реальные сделки).

```bash
# Запустить бота
php artisan bots:run
```

**Что вы должны увидеть:**
```
Starting trading bots...
------------------------------
Bot #1
Symbol: BTCUSDT
Position size (USDT): 5
Current price: 43250.5
RSI: 45.23
EMA: 43100.2
Signal: HOLD
Net position (BTC): 0
No action taken
All bots processed.
```

Если сигнал BUY:
```
Signal: BUY
DRY RUN BUY 5 USDT
```

Это нормально - бот показывает что бы сделал, но не совершает сделку.

### Шаг 10: Тест реальной сделки на testnet

**ВНИМАНИЕ**: Только если хотите протестировать реальную покупку на testnet!

```bash
php artisan tinker
```

```php
// Отключить dry_run для бота
>>> $bot = \App\Models\TradingBot::find(1);
>>> $bot->update(['dry_run' => false]);

// Проверить баланс (должен быть > 5 USDT)
>>> $account = \App\Models\ExchangeAccount::first();
>>> $bybit = new \App\Services\Exchanges\Bybit\BybitService($account);
>>> $bybit->getBalance('USDT');

>>> exit
```

```bash
# В .env установить (только для testnet!)
REAL_TRADING=true

# Очистить кэш
php artisan config:clear

# Запустить бота
php artisan bots:run
```

Теперь бот будет совершать реальные сделки на testnet (не настоящие деньги!).

### Шаг 11: Синхронизация ордеров

```bash
# Синхронизировать статусы ордеров
php artisan orders:sync
```

Эта команда:
- Проверяет статусы всех PENDING/SENT ордеров
- Обновляет их до FILLED если исполнены
- Закрывает позиции и считает PnL

**Запускайте каждую минуту** (или настройте cron).

### Шаг 12: Проверка результатов

```bash
php artisan tinker
```

```php
// Посмотреть все сделки
>>> \App\Models\Trade::latest()->get();

// Посмотреть только исполненные
>>> \App\Models\Trade::where('status', 'FILLED')->latest()->get();

// Статистика по боту
>>> $bot = \App\Models\TradingBot::find(1);
>>> $bot->trades()->where('status', 'FILLED')->count();
>>> $bot->trades()->sum('realized_pnl');

// Посмотреть открытые позиции
>>> \App\Models\Trade::where('side', 'BUY')
    ->where('status', 'FILLED')
    ->whereNull('closed_at')
    ->get();

>>> exit
```

---

## ✅ Чеклист проверки

### Базовая проверка:

- [ ] API ключи testnet созданы
- [ ] `.env` настроен (`BYBIT_ENV=testnet`, `REAL_TRADING=false`)
- [ ] `php artisan setup` выполнен успешно
- [ ] Баланс проверяется: `$bybit->getBalance('USDT')` работает
- [ ] Цена получается: `$bybit->getPrice('BTCUSDT')` работает
- [ ] Бот создан: `php artisan bybit-bot:create ...`
- [ ] Бот активирован: `is_active = true`
- [ ] `php artisan bots:run` запускается без ошибок
- [ ] В выводе видны: цена, RSI, EMA, сигнал
- [ ] `php artisan orders:sync` работает

### Проверка реальной торговли (опционально):

- [ ] Баланс testnet > суммы позиции
- [ ] `dry_run = false` у бота
- [ ] `REAL_TRADING=true` в .env
- [ ] Бот совершает реальные сделки на testnet
- [ ] Сделки видны в таблице `trades`
- [ ] Синхронизация обновляет статусы

---

## 🔍 Отладка проблем

### Проблема: "Failed to get balance"

**Причины:**
- Неправильные API ключи
- Неправильный формат ключей
- API ключи не имеют прав на Trade

**Решение:**
1. Проверьте ключи в `.env`
2. Убедитесь что ключи от testnet (не от production!)
3. Проверьте права API ключа (должны быть Read + Trade)
4. Выполните `php artisan config:clear`

### Проблема: "Invalid symbol"

**Причина:** Неправильный формат символа

**Решение:**
- Используйте формат: `BTCUSDT`, `ETHUSDT` (без `/`)
- Все буквы заглавные

### Проблема: "Insufficient balance"

**Причина:** Недостаточно USDT на testnet аккаунте

**Решение:**
1. Зайдите на https://testnet.bybit.com/
2. Получите тестовые USDT (обычно есть кнопка "Get Test Coins")
3. Или уменьшите `position_size` бота

### Проблема: Бот не совершает сделки

**Проверьте:**
1. `is_active = true` у бота
2. `dry_run = false` (если хотите реальные сделки)
3. `REAL_TRADING=true` в .env (если хотите реальные сделки)
4. Сигнал стратегии (может быть HOLD)
5. Баланс достаточен
6. Нет открытой позиции (для BUY)

---

## 📊 Мониторинг тестирования

### Следить за логами:

```bash
# В реальном времени
tail -f storage/logs/laravel.log

# Или использовать Laravel Pail
php artisan pail
```

### Что смотреть в логах:

- `BUY order initiated` - начало покупки
- `BUY order filled` - покупка исполнена
- `Insufficient balance` - недостаточно средств
- `Order sync error` - ошибка синхронизации

### Проверка в БД:

```bash
# Через Database Client в Cursor
# Или через tinker:

php artisan tinker
>>> \App\Models\Trade::latest()->take(5)->get(['id', 'side', 'status', 'price', 'quantity', 'created_at']);
```

---

## 🎯 Рекомендации по тестированию

1. **Начните с DRY RUN** - убедитесь что логика работает
2. **Тестируйте на малых суммах** - даже на testnet
3. **Следите за логами** - первые запуски особенно важны
4. **Проверяйте синхронизацию** - запускайте `orders:sync` регулярно
5. **Тестируйте разные сценарии**:
   - Разные символы (BTCUSDT, ETHUSDT)
   - Разные таймфреймы (5m, 15m, 1h)
   - Разные размеры позиций

---

## ✅ Готово к тестированию!

Следуйте инструкциям по порядку. Если что-то не работает - проверьте раздел "Отладка проблем".
