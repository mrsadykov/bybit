# Настройка Telegram уведомлений

## Получение токена бота

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и создайте бота:
   - Придумайте имя для бота (например: "Trading Bot")
   - Придумайте username для бота (например: "my_trading_bot") - должен заканчиваться на `bot`
4. Скопируйте токен бота (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
5. **Важно:** Запомните username вашего бота (например: `@my_trading_bot`)

## Запуск бота (обязательно!)

**Перед тем как получить Chat ID, нужно запустить бота:**

1. Найдите вашего бота в Telegram по username (например: `@my_trading_bot`)
   - Или откройте бота через ссылку, которую дал BotFather
2. Откройте чат с ботом
3. Нажмите кнопку **"START"** или отправьте команду `/start`
4. Бот ответит приветственным сообщением (это значит, что бот запущен)

**⚠️ Важно:** Без этого шага бот не сможет отправлять вам сообщения!

## Получение Chat ID

### Способ 1: Через бота @userinfobot

1. Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте ему любое сообщение
3. Бот вернет ваш Chat ID (число, например `123456789`)

### Способ 2: Через бота @getidsbot

1. Найдите бота [@getidsbot](https://t.me/getidsbot) в Telegram
2. Отправьте ему любое сообщение
3. Бот вернет ваш Chat ID

### Способ 3: Через API (если бот уже создан и запущен)

```bash
# Замените YOUR_BOT_TOKEN на токен вашего бота
curl https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates

# Найдите "chat":{"id":123456789} в ответе
# Это ваш Chat ID
```

**Важно:** После отправки `/start` вашему боту, его сообщение появится в ответе `getUpdates`.

### Способ 4: Через Artisan команду (рекомендуется!)

Самый простой способ - использовать встроенную команду:

```bash
php artisan telegram:chat-id
```

Или если токен не в .env:
```bash
php artisan telegram:chat-id --token=YOUR_BOT_TOKEN
```

**Важно:** Перед запуском команды:
1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start` команду
4. Подождите 2-3 секунды
5. Запустите команду `php artisan telegram:chat-id`

Команда автоматически найдет ваш Chat ID из последних обновлений.

### Способ 5: Прямо из API (вручную)

После того, как вы отправили `/start` вашему боту, можно получить Chat ID прямо из ответа API:

1. Отправьте `/start` вашему боту в Telegram
2. Подождите 2-3 секунды
3. Выполните команду (замените `YOUR_BOT_TOKEN`):
   ```bash
   curl https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates
   ```
4. Найдите `"chat":{"id":123456789}` в ответе - это ваш Chat ID

**⚠️ Важно:** Если `result` пустой `[]`, это значит:
- Либо вы еще не отправили `/start`
- Либо обновления уже были получены (Telegram удаляет их после чтения)

В этом случае отправьте `/start` снова и сразу же выполните команду.

## Настройка в .env

Добавьте в `.env` файл:

```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
```

## Тестирование

После настройки протестируйте отправку сообщений:

```bash
# Отправить все типы тестовых уведомлений
php artisan telegram:test

# Отправить кастомное сообщение
php artisan telegram:test --message="Мое тестовое сообщение"
```

Команда отправит:
1. Простое тестовое сообщение
2. BUY уведомление
3. SELL уведомление
4. FILLED уведомление
5. SKIP уведомление
6. POSITION CLOSED уведомление

Проверьте свой Telegram - все уведомления должны прийти!

## Что будет отправляться в Telegram

Бот будет отправлять уведомления о:

1. **BUY ордера:**
   - 🔵 DRY RUN BUY (тестовый режим)
   - 🟢 REAL BUY (реальная покупка)
   - ✅ BUY ORDER FILLED (ордер выполнен)

2. **SELL ордера:**
   - 🔵 DRY RUN SELL (тестовый режим)
   - 🟢 REAL SELL (реальная продажа)
   - 💰 SELL ORDER FILLED (ордер выполнен)

3. **Пропуски сделок:**
   - ⚠️ TRADE SKIPPED (с указанием причины)

4. **Ошибки:**
   - ❌ ERROR (с описанием ошибки)

## Отключение уведомлений

Если не хотите использовать Telegram уведомления, просто не указывайте токен и chat_id в `.env`:

```env
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=
```

Бот будет работать, но уведомления отправляться не будут (ошибки будут только в логах).
