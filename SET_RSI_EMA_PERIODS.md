# 🔧 Установка периодов RSI/EMA для ботов

## 📊 Текущее состояние

По скриншоту видно, что у всех ботов:
- `rsi_period` = NULL
- `ema_period` = NULL

Это означает, что используются дефолтные значения (RSI=17, EMA=10).

---

## 🎯 Рекомендуемые значения для ваших ботов

### Bot #1 (BTCUSDT):
- **RSI период:** 17
- **EMA период:** 10
- ✅ Текущие дефолтные значения подходят

### Bot #2 (ETHUSDT):
- **RSI период:** 17
- **EMA период:** 10-15 (рекомендуется 10 или 15)
- ⚠️ Можно оставить 10 или установить 15

### Bot #3 (SOLUSDT):
- **RSI период:** 17-20 (рекомендуется 17)
- **EMA период:** 15-20 (рекомендуется 15)
- ⚠️ **ВАЖНО:** Для SOL нужен EMA=15-20, не 10!

---

## ✅ Команды для установки периодов

### Вариант 1: Через tinker (рекомендуется)

```bash
php artisan tinker
```

```php
// Bot #1 (BTCUSDT) - дефолтные значения
$bot = \App\Models\TradingBot::find(1);
$bot->rsi_period = 17;
$bot->ema_period = 10;
$bot->save();
echo "✅ Bot #1 (BTCUSDT): RSI=17, EMA=10\n";

// Bot #2 (ETHUSDT) - можно оставить 10 или установить 15
$bot = \App\Models\TradingBot::find(2);
$bot->rsi_period = 17;
$bot->ema_period = 10; // или 15
$bot->save();
echo "✅ Bot #2 (ETHUSDT): RSI=17, EMA=10\n";

// Bot #3 (SOLUSDT) - ВАЖНО: EMA=15!
$bot = \App\Models\TradingBot::find(3);
$bot->rsi_period = 17;
$bot->ema_period = 15; // Рекомендуется 15 для SOL
$bot->save();
echo "✅ Bot #3 (SOLUSDT): RSI=17, EMA=15\n";

// Проверить результат
\App\Models\TradingBot::all()->each(function($bot) {
    $rsi = $bot->rsi_period ?? 17;
    $ema = $bot->ema_period ?? 10;
    echo "Bot #{$bot->id} ({$bot->symbol}): RSI={$rsi}, EMA={$ema}\n";
});

exit
```

### Вариант 2: Через SQL (быстро)

```sql
-- Bot #1 (BTCUSDT) - дефолтные
UPDATE trading_bots SET rsi_period = 17, ema_period = 10 WHERE id = 1;

-- Bot #2 (ETHUSDT) - дефолтные
UPDATE trading_bots SET rsi_period = 17, ema_period = 10 WHERE id = 2;

-- Bot #3 (SOLUSDT) - ВАЖНО: EMA=15!
UPDATE trading_bots SET rsi_period = 17, ema_period = 15 WHERE id = 3;
```

### Вариант 3: Массовое обновление (если нужно одинаковое для всех)

```bash
php artisan tinker
```

```php
// Установить дефолтные для всех
\App\Models\TradingBot::whereNull('rsi_period')->update(['rsi_period' => 17]);
\App\Models\TradingBot::whereNull('ema_period')->update(['ema_period' => 10]);

// Затем обновить SOLUSDT отдельно
$sol = \App\Models\TradingBot::where('symbol', 'SOLUSDT')->first();
$sol->ema_period = 15;
$sol->save();

exit
```

---

## 🔍 Проверка после установки

```bash
php artisan tinker
```

```php
\App\Models\TradingBot::all()->each(function($bot) {
    $rsi = $bot->rsi_period ?? 17;
    $ema = $bot->ema_period ?? 10;
    echo "Bot #{$bot->id} ({$bot->symbol}): RSI={$rsi}, EMA={$ema}\n";
});

exit
```

**Ожидаемый результат:**
```
Bot #1 (BTCUSDT): RSI=17, EMA=10
Bot #2 (ETHUSDT): RSI=17, EMA=10
Bot #3 (SOLUSDT): RSI=17, EMA=15
```

---

## ⚠️ Важно для SOLUSDT

SOLUSDT более волатилен, чем BTC и ETH, поэтому:
- ✅ **EMA=15** (не 10!) - более медленная реакция, меньше ложных сигналов
- ✅ Можно попробовать **EMA=20** для еще более стабильной работы
- ✅ **RSI=17** подходит (можно попробовать 20)

---

## 📋 Итоговая таблица

| Бот | Символ | RSI | EMA | Примечание |
|-----|--------|-----|-----|------------|
| #1 | BTCUSDT | 17 | 10 | Дефолтные значения |
| #2 | ETHUSDT | 17 | 10 | Дефолтные значения (можно 15) |
| #3 | SOLUSDT | 17 | **15** | ⚠️ ВАЖНО: EMA=15, не 10! |

---

## ✅ После установки

Боты автоматически начнут использовать новые периоды при следующем запуске `bots:run`. Проверьте логи:

```bash
php artisan bots:run
```

Должно показывать:
```
RSI (17): 50.73
EMA (15): 128.22  # Для SOLUSDT
```
