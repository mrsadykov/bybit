# Работа с Production (bybit.com)

## ⚠️ ВАЖНО: Безопасность при работе с реальными деньгами

### 1. Настройки безопасности

**В `.env` файле:**
```env
# Отключите реальную торговлю для тестирования
REAL_TRADING=false

# Или используйте dry_run в боте
```

**В коде бота:**
- Убедитесь что `dry_run = true` для тестовых ботов
- Проверьте что `REAL_TRADING=false` в config

### 2. Рекомендации перед запуском на production

1. **Начните с минимальных сумм:**
   - Используйте очень маленький `position_size` (например, 0.00001 USDT)
   - Тестируйте на одной паре

2. **Проверьте все настройки:**
   ```bash
   # Проверьте баланс
   php artisan balance:check USDT --production
   
   # Проверьте API подключение
   php artisan api:test --account=PRODUCTION_ID
   ```

3. **Создайте тестового бота с dry_run:**
   ```bash
   php artisan bybit-bot:create BTCUSDT 5m rsi_ema 0.00001
   ```
   
   Затем в БД установите `dry_run = true` для этого бота

4. **Мониторинг:**
   - Следите за логами: `tail -f storage/logs/laravel.log`
   - Проверяйте статусы ордеров: `php artisan orders:sync`
   - Следите за балансом регулярно

### 3. Защита от ошибок

**В `config/trading.php`:**
```php
'real_trading' => env('REAL_TRADING', false), // По умолчанию false!
'min_notional_usdt' => 1, // Минимальная сумма сделки
```

**В коде бота:**
- Всегда проверяйте баланс перед покупкой
- Проверяйте минимальную сумму сделки
- Логируйте все действия

### 4. Постепенный запуск

1. **Этап 1: Dry Run (без реальных сделок)**
   - `REAL_TRADING=false`
   - `dry_run=true` в боте
   - Проверка логики без реальных сделок

2. **Этап 2: Минимальные суммы**
   - `REAL_TRADING=true`
   - Очень маленький `position_size`
   - Одна торговая пара
   - Мониторинг каждой сделки

3. **Этап 3: Постепенное увеличение**
   - Увеличивайте `position_size` постепенно
   - Добавляйте новые пары по одной
   - Продолжайте мониторинг

### 5. Что делать если testnet не работает

1. **Продолжайте разработку на production с dry_run:**
   - Создайте бота с `dry_run = true`
   - Тестируйте логику без реальных сделок
   - Проверяйте что все работает правильно

2. **Когда будете готовы к реальной торговле:**
   - Начните с минимальных сумм
   - Используйте только один бот
   - Мониторьте каждую сделку

3. **Вернитесь к testnet когда он заработает:**
   - Переключитесь на testnet для тестирования новых функций
   - Используйте production только для реальной торговли

## 🛡️ Защита от потерь

1. **Лимиты:**
   - Установите максимальную сумму на сделку
   - Установите максимальное количество сделок в день
   - Установите стоп-лосс

2. **Мониторинг:**
   - Настройте алерты на большие сделки
   - Регулярно проверяйте баланс
   - Следите за логами

3. **Резервное копирование:**
   - Регулярно делайте бэкап БД
   - Сохраняйте логи важных сделок

## ✅ Чеклист перед запуском на production

- [ ] `REAL_TRADING=false` в .env (для начала)
- [ ] `dry_run=true` в боте
- [ ] Минимальный `position_size`
- [ ] Проверен баланс
- [ ] Проверено API подключение
- [ ] Настроен мониторинг
- [ ] Понимаете риски
- [ ] Готовы к потерям (даже при правильной настройке)
