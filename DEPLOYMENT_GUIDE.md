# Руководство по деплою торгового бота

## ✅ Что готово

1. **Основной функционал:**
   - ✅ Автоматическая торговля (BUY/SELL) на основе RSI + EMA стратегии
   - ✅ Поддержка OKX и Bybit бирж
   - ✅ Синхронизация статусов ордеров (`orders:sync`)
   - ✅ Восстановление ордеров с биржи (`trades:recover`)
   - ✅ Защита от двойных ордеров
   - ✅ Проверка баланса перед сделками
   - ✅ Режим dry_run для безопасного тестирования
   - ✅ Логирование всех операций

2. **Безопасность:**
   - ✅ Проверка `dry_run` перед реальными сделками
   - ✅ Проверка баланса перед BUY
   - ✅ Проверка наличия позиции перед SELL
   - ✅ Защита от двойных SELL ордеров
   - ✅ Минимальная сумма сделки (min_notional_usdt)

## 📋 План деплоя на сервер

### 1. Подготовка сервера

```bash
# Установка зависимостей
composer install --no-dev --optimize-autoloader
npm install && npm run build

# Настройка окружения
cp .env.example .env
php artisan key:generate
```

### 2. Настройка .env файла

```env
# Торговля
REAL_TRADING=true  # Включить реальную торговлю
APP_ENV=production

# OKX API (если используете OKX)
OKX_API_PASSPHRASE=your_passphrase

# База данных
DB_CONNECTION=mysql  # или sqlite для простоты
DB_DATABASE=your_database
DB_USERNAME=your_user
DB_PASSWORD=your_password

# Логирование
LOG_CHANNEL=daily  # или stack для детального логирования
LOG_LEVEL=info
```

### 3. Настройка базы данных

```bash
# Миграции
php artisan migrate --force

# Создание аккаунта биржи (OKX)
php artisan create-okx-account

# Создание торгового бота
php artisan create-okx-trading-bot
```

### 4. Настройка бота в базе данных

```sql
-- Проверить настройки бота
SELECT id, symbol, position_size, dry_run, is_active 
FROM trading_bots;

-- ВАЖНО: Установить dry_run = 1 для первого запуска!
UPDATE trading_bots SET dry_run = 1 WHERE id = 24;

-- После проверки в dry_run режиме:
UPDATE trading_bots SET dry_run = 0 WHERE id = 24;
```

### 5. Настройка автоматического запуска (Cron)

Добавьте в crontab (`crontab -e`):

```bash
# Запуск ботов каждые 5 минут
*/5 * * * * cd /path/to/project && php artisan bots:run >> /dev/null 2>&1

# Синхронизация статусов ордеров каждую минуту
* * * * * cd /path/to/project && php artisan orders:sync >> /dev/null 2>&1
```

Или используйте Laravel Scheduler (если настроен):

```php
// app/Console/Kernel.php
protected function schedule(Schedule $schedule)
{
    $schedule->command('bots:run')->everyFiveMinutes();
    $schedule->command('orders:sync')->everyMinute();
}
```

### 6. Первый запуск (тестирование)

```bash
# 1. Проверить подключение к бирже
php artisan test-okx-connection

# 2. Проверить баланс
php artisan check-balance

# 3. Запустить в dry_run режиме
php artisan bots:run
# Должно показать "DRY RUN BUY" или "DRY RUN SELL"

# 4. Проверить логи
tail -f storage/logs/laravel.log
```

### 7. Включение реальной торговли

**ВАЖНО:** Только после проверки в dry_run режиме!

```bash
# 1. Установить REAL_TRADING=true в .env
# 2. Установить dry_run = 0 в базе данных
UPDATE trading_bots SET dry_run = 0 WHERE id = 24;

# 3. Запустить бота
php artisan bots:run
```

### 8. Мониторинг

```bash
# Просмотр логов
tail -f storage/logs/laravel.log

# Проверка статуса ордеров
php artisan orders:sync

# Восстановление ордеров (если нужно)
php artisan trades:recover

# Проверка баланса
php artisan check-balance
```

## ⚠️ Важные моменты

1. **Всегда начинайте с dry_run = 1**
   - Проверьте работу бота в тестовом режиме
   - Убедитесь, что стратегия работает корректно
   - Проверьте логи на наличие ошибок

2. **Мониторинг баланса**
   - Регулярно проверяйте баланс на бирже
   - Убедитесь, что достаточно средств для торговли
   - Минимальная сумма: `min_notional_usdt` (по умолчанию 1 USDT)

3. **Логирование**
   - Все операции логируются в `storage/logs/laravel.log`
   - Проверяйте логи регулярно на наличие ошибок
   - Настройте ротацию логов для production

4. **Резервное копирование**
   - Регулярно делайте бэкап базы данных
   - Сохраняйте важные логи

5. **API ключи**
   - Храните API ключи в безопасности
   - Используйте только необходимые права (Read, Trade)
   - НЕ используйте права на вывод средств (Withdraw)

## 🔧 Команды для управления

```bash
# Запуск ботов
php artisan bots:run

# Синхронизация статусов
php artisan orders:sync

# Восстановление ордеров
php artisan trades:recover

# Проверка баланса
php artisan check-balance

# Тест подключения
php artisan test-okx-connection
```

## 📊 Стратегия торговли

Текущая стратегия: **RSI + EMA**
- **BUY:** RSI < 30 И цена > EMA(20)
- **SELL:** RSI > 70 И цена < EMA(20)
- **HOLD:** Во всех остальных случаях

Параметры можно изменить в `app/Trading/Strategies/RsiEmaStrategy.php`

## 🚨 Что делать при проблемах

1. **Бот не торгует:**
   - Проверьте `is_active = 1` в базе данных
   - Проверьте `dry_run` режим
   - Проверьте баланс на бирже
   - Проверьте логи на ошибки

2. **Ордера не синхронизируются:**
   - Запустите `php artisan orders:sync` вручную
   - Проверьте подключение к бирже
   - Проверьте логи

3. **Ошибки API:**
   - Проверьте API ключи
   - Проверьте права API ключей
   - Проверьте лимиты API биржи

## ✅ Чеклист перед запуском

- [ ] База данных настроена и миграции выполнены
- [ ] API ключи биржи настроены
- [ ] Торговый бот создан в базе данных
- [ ] `dry_run = 1` для первого запуска
- [ ] `REAL_TRADING=false` в .env для тестирования
- [ ] Баланс на бирже достаточен для торговли
- [ ] Cron настроен для автоматического запуска
- [ ] Логирование настроено
- [ ] Резервное копирование настроено
- [ ] Мониторинг настроен

## 🎯 После успешного тестирования

1. Установить `REAL_TRADING=true` в .env
2. Установить `dry_run = 0` в базе данных
3. Запустить бота и проверить первую сделку
4. Мониторить работу в течение первых часов
5. Регулярно проверять логи и баланс
