# 🔧 Команды для ручной покупки и продажи

## 🎯 Назначение:

Команды для тестирования покупки и продажи вручную, без ожидания сигналов от стратегии.

---

## 📋 Команды:

### 1. `trade:buy` - Ручная покупка

**Синтаксис:**
```bash
php artisan trade:buy {symbol} {amount} [--bot=ID] [--dry-run]
```

**Параметры:**
- `symbol` - Торговая пара (например: BTCUSDT, ETHUSDT)
- `amount` - Сумма в USDT для покупки (например: 10)
- `--bot=ID` - ID бота (опционально, использует первый активный бот)
- `--dry-run` - Тестовый режим (не создает реальный ордер)

**Примеры:**

```bash
# Реальная покупка BTC на 10 USDT
php artisan trade:buy BTCUSDT 10

# Тестовая покупка (dry_run)
php artisan trade:buy BTCUSDT 10 --dry-run

# Покупка с указанием бота
php artisan trade:buy BTCUSDT 10 --bot=1
```

**Что делает:**
1. Получает текущую цену
2. Проверяет баланс USDT (если не dry-run)
3. Создает ордер на покупку
4. Ожидает исполнения ордера
5. Обновляет статус в БД
6. Отправляет уведомление в Telegram

**Ожидаемый вывод:**
```
Ручная покупка (Manual BUY order)
Бот (Bot): #1
Символ (Symbol): BTCUSDT
Сумма (Amount): 10 USDT
Режим (Mode): РЕАЛЬНАЯ ТОРГОВЛЯ (REAL TRADING)

Текущая цена (Current price): 95326.1
Баланс USDT (USDT Balance): 100.5
РЕАЛЬНАЯ ПОКУПКА ВЫПОЛНЯЕТСЯ (REAL BUY EXECUTING) (10 USDT)
ОРДЕР BUY ИСПОЛНЕН (BUY ORDER FILLED)
Количество (Quantity): 0.000105 BTC
Комиссия (Fee): 0.01 USDT
```

---

### 2. `trade:sell` - Ручная продажа

**Синтаксис:**
```bash
php artisan trade:sell {symbol} {quantity} [--bot=ID] [--dry-run] [--all]
```

**Параметры:**
- `symbol` - Торговая пара (например: BTCUSDT, ETHUSDT)
- `quantity` - Количество для продажи (например: 0.001)
- `--bot=ID` - ID бота (опционально, использует первый активный бот)
- `--dry-run` - Тестовый режим (не создает реальный ордер)
- `--all` - Продать весь доступный баланс (игнорирует quantity)

**Примеры:**

```bash
# Реальная продажа 0.001 BTC
php artisan trade:sell BTCUSDT 0.001

# Продать весь доступный BTC баланс
php artisan trade:sell BTCUSDT 0 --all

# Тестовая продажа (dry_run)
php artisan trade:sell BTCUSDT 0.001 --dry-run

# Продажа с указанием бота
php artisan trade:sell BTCUSDT 0.001 --bot=1
```

**Что делает:**
1. Получает текущую цену
2. Определяет количество для продажи (или весь баланс, если `--all`)
3. Проверяет баланс (если не dry-run)
4. Находит открытый BUY для parent_id (FIFO)
5. Создает ордер на продажу
6. Ожидает исполнения ордера
7. Обновляет статус в БД
8. Отправляет уведомление в Telegram

**Ожидаемый вывод:**
```
Ручная продажа (Manual SELL order)
Бот (Bot): #1
Символ (Symbol): BTCUSDT
Количество (Quantity): 0.001 BTC
Режим (Mode): РЕАЛЬНАЯ ТОРГОВЛЯ (REAL TRADING)

Текущая цена (Current price): 95326.1
Доступный баланс BTC (Available BTC balance): 0.00105
РЕАЛЬНАЯ ПРОДАЖА ВЫПОЛНЯЕТСЯ (REAL SELL EXECUTING) (0.001 BTC)
ОРДЕР SELL ИСПОЛНЕН (SELL ORDER FILLED)
Цена исполнения (Execution price): 95326.1 USDT
Комиссия (Fee): 0.001 USDT
Запустите php artisan orders:sync для расчета PnL и закрытия позиции
```

---

## 🧪 Тестирование на сервере:

### Шаг 1: Тестовая покупка (dry-run):

```bash
# На сервере
cd /var/www/trading-bot

# Тестовая покупка (не создает реальный ордер)
php artisan trade:buy BTCUSDT 10 --dry-run
```

**Что проверить:**
- ✅ Команда выполняется без ошибок
- ✅ Показывает правильную цену
- ✅ Показывает "ТЕСТОВЫЙ РЕЖИМ BUY (DRY RUN BUY)"
- ✅ Telegram уведомление приходит

---

### Шаг 2: Реальная покупка (если тест прошел):

```bash
# Реальная покупка (создает реальный ордер)
php artisan trade:buy BTCUSDT 10
```

**Что проверить:**
- ✅ Команда выполняется без ошибок
- ✅ Ордер создается на бирже
- ✅ Статус обновляется в БД
- ✅ Telegram уведомление о реальной покупке

**После покупки:**
```bash
# Синхронизировать ордера
php artisan orders:sync

# Проверить сделку в БД
php artisan tinker
>>> \App\Models\Trade::latest()->first();
>>> exit
```

---

### Шаг 3: Тестовая продажа (dry-run):

```bash
# Тестовая продажа (не создает реальный ордер)
php artisan trade:sell BTCUSDT 0.001 --dry-run
```

**Или продать все:**
```bash
php artisan trade:sell BTCUSDT 0 --all --dry-run
```

**Что проверить:**
- ✅ Команда выполняется без ошибок
- ✅ Показывает правильную цену и баланс
- ✅ Показывает "ТЕСТОВЫЙ РЕЖИМ SELL (DRY RUN SELL)"
- ✅ Telegram уведомление приходит

---

### Шаг 4: Реальная продажа (если тест прошел):

```bash
# Реальная продажа всего баланса
php artisan trade:sell BTCUSDT 0 --all
```

**Или указать количество:**
```bash
php artisan trade:sell BTCUSDT 0.001
```

**Что проверить:**
- ✅ Команда выполняется без ошибок
- ✅ Ордер создается на бирже
- ✅ Статус обновляется в БД
- ✅ Telegram уведомление о реальной продаже

**После продажи:**
```bash
# Синхронизировать ордера и рассчитать PnL
php artisan orders:sync

# Проверить сделку и PnL в БД
php artisan tinker
>>> $sell = \App\Models\Trade::where('side', 'SELL')->latest()->first();
>>> echo "SELL #{$sell->id}: Price: {$sell->price}, PnL: {$sell->realized_pnl}\n";
>>> $buy = \App\Models\Trade::find($sell->parent_id);
>>> echo "BUY #{$buy->id}: Price: {$buy->price}, Closed: " . ($buy->closed_at ? 'ДА' : 'НЕТ') . "\n";
>>> exit
```

---

## ⚠️ Важные замечания:

### 1. Проверка баланса:

**Перед покупкой:**
- Убедитесь, что баланс USDT достаточен
- Минимум: размер покупки + 5 USDT (запас)

**Перед продажей:**
- Убедитесь, что баланс BTC достаточен
- Используйте `--all` для продажи всего баланса

### 2. Тестовый режим (dry-run):

**Что делает:**
- ✅ Проверяет баланс и цену
- ✅ Показывает, что было бы сделано
- ✅ Отправляет Telegram уведомление
- ❌ НЕ создает реальный ордер на бирже

**Когда использовать:**
- Для проверки работы команды
- Для проверки баланса и цены
- Для проверки Telegram уведомлений

### 3. Реальная торговля:

**Что делает:**
- ✅ Создает реальный ордер на бирже
- ✅ Использует реальные средства
- ✅ Обновляет БД
- ✅ Отправляет Telegram уведомления

**⚠️ ВНИМАНИЕ:**
- Реальная торговля использует реальные деньги
- Проверьте баланс перед использованием
- Начните с небольших сумм для тестирования

---

## 📊 Полный цикл тестирования:

### 1. Тестовая покупка:
```bash
php artisan trade:buy BTCUSDT 10 --dry-run
```

### 2. Реальная покупка:
```bash
php artisan trade:buy BTCUSDT 10
```

### 3. Проверка покупки:
```bash
php artisan orders:sync
php artisan tinker
>>> \App\Models\Trade::where('side', 'BUY')->latest()->first();
```

### 4. Тестовая продажа:
```bash
php artisan trade:sell BTCUSDT 0 --all --dry-run
```

### 5. Реальная продажа:
```bash
php artisan trade:sell BTCUSDT 0 --all
```

### 6. Проверка продажи и PnL:
```bash
php artisan orders:sync
php artisan tinker
>>> \App\Models\Trade::latest()->take(5)->get();
```

---

## 🎯 Рекомендуемая последовательность для тестирования:

1. ✅ **Тестовая покупка** (`--dry-run`)
   - Проверить работу команды
   - Проверить Telegram уведомления

2. ✅ **Реальная покупка** (маленькая сумма, например 5 USDT)
   - Проверить создание ордера
   - Проверить синхронизацию

3. ✅ **Проверка покупки**
   - `orders:sync` для обновления статуса
   - Проверить сделку в БД

4. ✅ **Тестовая продажа** (`--dry-run`)
   - Проверить работу команды
   - Проверить Telegram уведомления

5. ✅ **Реальная продажа** (`--all` или указать количество)
   - Проверить создание ордера
   - Проверить синхронизацию

6. ✅ **Проверка продажи и PnL**
   - `orders:sync` для расчета PnL
   - Проверить закрытые позиции
   - Проверить PnL в БД

---

## ❓ Частые вопросы:

**Q: Можно ли использовать команды с разными ботами?**  
A: Да, используйте `--bot=ID` для указания конкретного бота.

**Q: Что делать, если ордер не исполнен сразу?**  
A: Запустите `php artisan orders:sync` для проверки статуса.

**Q: Как проверить PnL после продажи?**  
A: Запустите `php artisan orders:sync`, затем проверьте `realized_pnl` в БД.

**Q: Можно ли использовать команды в тестовом режиме?**  
A: Да, используйте `--dry-run` для тестового режима.

**Q: Что делать, если недостаточно баланса?**  
A: Команда покажет ошибку с текущим балансом. Пополните счет или уменьшите сумму.

---

## 📚 Дополнительная информация:

- `CHECK_TRADES_COMMANDS.md` - Команды для проверки сделок
- `PRE_REAL_TRADING_CHECKLIST.md` - Чек-лист перед реальной торговлей
- `LOGS_LOCATION.md` - Где находятся логи

---

## ⚠️ ДИСКЛЕЙМЕР:

Команды для ручной покупки и продажи используют реальные средства при использовании без флага `--dry-run`. Всегда проверяйте баланс и параметры перед выполнением реальных сделок. Начинайте с небольших сумм для тестирования.
