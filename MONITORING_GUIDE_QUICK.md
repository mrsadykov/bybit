# 🔍 Быстрый гайд по мониторингу торговой системы

## 🚀 Основные команды для мониторинга

### 1. Полный мониторинг системы

```bash
php artisan monitor:system
```

**Что показывает:**
- ✅ Статус всех активных ботов
- ✅ Последние сделки
- ✅ Ошибки за последние 7 дней
- ✅ Балансы и позиции
- ✅ Проверка минимальных размеров ордеров

**Пример вывода:**
```
📊 Статус ботов (Bots Status)
  Bot #1: BTCUSDT
    Размер позиции (Position Size): 10 USDT
    Успешных (Filled): 5
    ❌ Ошибок (Failed): 0
    Открытых позиций (Open Positions): 1

💰 Балансы и позиции (Balances & Positions)
  Bot #1: BTCUSDT
    Баланс на бирже (Exchange Balance): 0.000112 BTC
    ✅ Балансы синхронизированы (Balances synced)

📏 Проверка минимальных размеров (Min Order Size Check)
  Bot #1: BTCUSDT
    ✅ Баланс (0.000112 BTC) >= минимума (0.0001 BTC)
    ✅ Position size (10 USDT) даст ~0.000112 BTC (>= 0.0001)
```

---

### 2. Мониторинг конкретного бота

```bash
php artisan monitor:system --bot=1
```

Показывает информацию только для бота с ID=1.

---

### 3. Только ошибки

```bash
php artisan monitor:system --errors
```

Показывает только ошибки за последние 7 дней, сгруппированные по типам.

---

### 4. Последние сделки

```bash
php artisan monitor:system --recent
```

Показывает последние 10 сделок со статусами.

---

## 📊 Другие полезные команды

### Проверка баланса

```bash
# Проверить баланс USDT
php artisan balance:check USDT

# Проверить баланс BTC
php artisan balance:check BTC

# Проверить конкретный аккаунт
php artisan balance:check USDT --account=1
```

### Проверка статуса ботов

```bash
php artisan bots:check
```

Проверяет корректность настроек всех ботов.

### Проверка последних сделок через tinker

```bash
php artisan tinker
```

```php
// Последние 10 сделок
\App\Models\Trade::latest()->take(10)->get();

// Только ошибки
\App\Models\Trade::where('status', 'FAILED')->latest()->get();

// Открытые позиции
\App\Models\Trade::where('side', 'BUY')
    ->where('status', 'FILLED')
    ->whereNull('closed_at')
    ->get();

// Статистика по боту
$bot = \App\Models\TradingBot::find(1);
$bot->trades()->where('status', 'FILLED')->count();
```

---

## ⚠️ На что обращать внимание

### 1. Ошибки "Parameter sz error"

**Симптом:**
```
❌ Ошибки (Errors)
  Parameter sz error: 5 раз(а)
    ⚠️  Количество (0.00005764) меньше минимального размера ордера
```

**Решение:**
- Увеличить `position_size` для бота в БД
- Или накопить больше монет перед продажей

### 2. Разница между балансом и позицией

**Симптом:**
```
⚠️  Разница (Difference): 0.00005 BTC
```

**Решение:**
- Запустить синхронизацию: `php artisan orders:sync`
- Или: `php artisan position:sync-from-balance`

### 3. Много FAILED сделок

**Симптом:**
```
❌ Ошибок (Failed): 10
```

**Действия:**
1. Проверить детали ошибок: `php artisan monitor:system --errors`
2. Проверить баланс: `php artisan balance:check USDT`
3. Проверить настройки бота: `php artisan bots:check`

---

## 📅 Рекомендуемый график мониторинга

### Ежедневно (1 раз в день)

```bash
php artisan monitor:system
```

Проверяйте:
- ✅ Нет ли новых ошибок
- ✅ Балансы синхронизированы
- ✅ Открытые позиции корректны

### При появлении ошибок

```bash
php artisan monitor:system --errors
```

Проверяйте:
- ❌ Тип ошибок
- ❌ Частота ошибок
- ❌ Последняя ошибка

### После изменений настроек

```bash
php artisan bots:check
php artisan monitor:system --bot=1
```

Проверяйте:
- ✅ Настройки корректны
- ✅ Минимальные размеры правильные
- ✅ Балансы достаточны

---

## 🔔 Telegram уведомления

Система автоматически отправляет уведомления в Telegram:
- ✅ Запуск ботов
- ✅ BUY/SELL сделки
- ✅ Ошибки
- ✅ HOLD сигналы

**Проверка Telegram:**
```bash
php artisan telegram:test
```

---

## 📈 Что считается нормальным

### ✅ Нормально:
- 0-2 ошибки в неделю (временные проблемы API)
- Балансы синхронизированы (разница < 0.0001)
- Все новые сделки проходят успешно
- Открытые позиции соответствуют балансу

### ⚠️ Требует внимания:
- 3-5 ошибок в неделю
- Разница балансов > 0.0001
- Повторяющиеся ошибки одного типа
- Много FAILED сделок для одного бота

### ❌ Критично:
- Множественные ошибки "Parameter sz error"
- Балансы не синхронизируются
- Все сделки FAILED
- Ошибки подключения к API

---

## 🛠️ Быстрые команды для диагностики

```bash
# Полный статус
php artisan monitor:system

# Только ошибки
php artisan monitor:system --errors

# Конкретный бот
php artisan monitor:system --bot=1

# Проверка баланса
php artisan balance:check USDT

# Проверка ботов
php artisan bots:check

# Синхронизация ордеров
php artisan orders:sync

# Тест Telegram
php artisan telegram:test
```

---

## 💡 Советы

1. **Запускайте мониторинг регулярно** - хотя бы раз в день
2. **Проверяйте ошибки** - особенно после изменений
3. **Следите за балансами** - они должны синхронизироваться
4. **Проверяйте минимальные размеры** - особенно для BTCUSDT
5. **Используйте Telegram** - уведомления помогут быстро реагировать

---

**Дата создания:** 28.01.2026  
**Версия:** 1.0
