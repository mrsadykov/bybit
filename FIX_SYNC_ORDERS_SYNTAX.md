# 🔧 Исправление синтаксической ошибки в SyncOrdersCommand

## 📊 Проблема

Команда `orders:sync` не обнаруживалась Laravel из-за синтаксической ошибки в файле `SyncOrdersCommand.php` на строке 313.

**Ошибка:**
```
PHP Parse error: syntax error, unexpected token "catch" in app/Console/Commands/SyncOrdersCommand.php on line 313
```

## ✅ Решение

Исправлена структура блоков `if` в файле:
- Блок закрытия позиций (строки 193-283) теперь правильно находится внутри блока `if ($isFilled || $isPartiallyFilled)`
- Удалена лишняя закрывающая скобка

## 📋 Команды для выполнения на проде

После деплоя исправленного файла:

```bash
cd /var/www/trading-bot
git pull
composer dump-autoload
php artisan config:clear
php artisan list | grep sync
```

Должна появиться команда:
```
  orders:sync              Sync pending exchange orders
```

## 🚀 Использование

После исправления можно использовать команду для синхронизации ордеров:

```bash
php artisan orders:sync
```

Эта команда:
- Синхронизирует статусы ордеров с биржи
- Закрывает позиции для FILLED SELL ордеров
- Связывает SELL с BUY по FIFO
- Рассчитывает PnL для закрытых позиций

## 💡 Альтернатива: Закрыть позицию вручную

Если нужно быстро закрыть позицию BNBUSDT, можно использовать tinker (см. `CLOSE_BNB_POSITION_MANUAL.md`).
