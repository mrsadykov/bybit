# Обновление параметров торговых ботов

## ✅ Что было исправлено

1. **Добавлены настраиваемые пороги RSI в бэктестинг:**
   - `--rsi-buy-threshold` (по умолчанию 30, теперь можно настроить)
   - `--rsi-sell-threshold` (по умолчанию 70, теперь можно настроить)

2. **Обновлена стратегия RsiEmaStrategy:**
   - Теперь использует более мягкие пороги по умолчанию: **40/60** вместо 30/70
   - Это даст больше торговых возможностей в реальной торговле

3. **Обновлен BacktestAllBotsCommand:**
   - Автоматически использует пороги 40/60 при бэктестинге всех ботов

---

## 🚀 Что делать дальше

### Шаг 1: Запустить бэктестинг с новыми параметрами

```bash
php artisan strategy:backtest-all --period=500 --exchange=okx --output=backtest_results.json
```

Теперь должно быть **больше сделок** (не 0), так как пороги стали мягче (40/60 вместо 30/70).

### Шаг 2: Проанализировать результаты

Проверьте:
- ✅ Количество сделок (должно быть > 0)
- ✅ Win Rate (процент прибыльных сделок)
- ✅ Доходность (Return %)
- ✅ Total PnL

### Шаг 3: Оптимизировать параметры (опционально)

Если результаты неудовлетворительные, попробуйте разные комбинации:

```bash
# Более консервативно (35/65)
php artisan strategy:backtest BTCUSDT --timeframe=1h --exchange=okx \
  --period=500 --rsi-period=17 --ema-period=10 \
  --rsi-buy-threshold=35 --rsi-sell-threshold=65 \
  --position-size=5 --stop-loss=3 --take-profit=6 --json

# Более агрессивно (45/55)
php artisan strategy:backtest BTCUSDT --timeframe=1h --exchange=okx \
  --period=500 --rsi-period=17 --ema-period=10 \
  --rsi-buy-threshold=45 --rsi-sell-threshold=55 \
  --position-size=5 --stop-loss=3 --take-profit=6 --json
```

### Шаг 4: Применить к реальным ботам

**Хорошая новость:** Стратегия уже обновлена! Все боты автоматически будут использовать новые пороги (40/60) при следующем запуске `php artisan bots:run`.

**Если хотите вернуться к старым порогам (30/70):**
- Можно обновить `RsiEmaStrategy.php` обратно
- Или добавить поля в БД для настройки порогов для каждого бота

---

## 📊 Ожидаемые результаты

### До изменений:
- `total_trades: 0` (слишком строгие условия)

### После изменений:
- `total_trades: > 0` (больше торговых возможностей)
- Более реалистичные результаты бэктестинга
- Больше сигналов в реальной торговле

---

## ⚠️ Важные замечания

1. **Больше сделок ≠ лучше:**
   - Больше сделок = больше комиссий
   - Нужен баланс между количеством и качеством

2. **Тестируйте перед реальной торговлей:**
   - Запустите бэктестинг с разными параметрами
   - Выберите лучшие параметры для каждой пары
   - Начните с маленьких сумм

3. **Мониторинг:**
   - Следите за результатами в реальной торговле
   - При необходимости корректируйте параметры
   - Используйте Stop-Loss и Take-Profit для защиты капитала

---

## 🔧 Дополнительные настройки

Если хотите настроить пороги для каждого бота индивидуально, можно:

1. **Добавить поля в БД:**
   ```sql
   ALTER TABLE trading_bots 
   ADD COLUMN rsi_buy_threshold DECIMAL(5,2) DEFAULT 40.0,
   ADD COLUMN rsi_sell_threshold DECIMAL(5,2) DEFAULT 60.0;
   ```

2. **Обновить RunTradingBotsCommand:**
   - Читать пороги из БД
   - Передавать их в `RsiEmaStrategy::decide()`

3. **Добавить в админ-панель:**
   - Поля для настройки порогов RSI для каждого бота

---

## 📝 Резюме

✅ **Стратегия обновлена** - теперь использует пороги 40/60 по умолчанию  
✅ **Бэктестинг обновлен** - можно настраивать пороги через параметры  
✅ **Готово к использованию** - запустите бэктестинг и проанализируйте результаты

**Следующий шаг:** Запустите `php artisan strategy:backtest-all --period=500 --exchange=okx` и посмотрите результаты!
