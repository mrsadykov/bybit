<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Health check: максимальный возраст последнего успешного запуска ботов (минуты).
    | Если bots:run / futures:run / btc-quote:run не выполнялись успешно дольше — алерт.
    |--------------------------------------------------------------------------
    */
    'last_run_max_minutes' => (int) env('HEALTH_LAST_RUN_MAX_MINUTES', 15),

    /*
    |--------------------------------------------------------------------------
    | Кулдаун алерта (минуты): не слать один и тот же алерт чаще чем раз в N минут.
    |--------------------------------------------------------------------------
    */
    'alert_cooldown_minutes' => (int) env('HEALTH_ALERT_COOLDOWN_MINUTES', 60),

    /*
    |--------------------------------------------------------------------------
    | Проверять OKX Spot API (getPrice).
    |--------------------------------------------------------------------------
    */
    'check_okx_spot' => env('HEALTH_CHECK_OKX_SPOT', true),

    /*
    |--------------------------------------------------------------------------
    | Проверять OKX Futures API (если есть активные фьючерсные боты).
    |--------------------------------------------------------------------------
    */
    'check_okx_futures' => env('HEALTH_CHECK_OKX_FUTURES', true),

    /*
    |--------------------------------------------------------------------------
    | Проверять Telegram API (getMe).
    |--------------------------------------------------------------------------
    */
    'check_telegram' => env('HEALTH_CHECK_TELEGRAM', true),
];
